{
 "schema_version": 1,
 "prd": "PRD.md",
 "product_version": "1.0.0",
 "updated_at": "2026-09-07T05:13:55Z",
 "status_values": [
  "Complete",
  "Partial",
  "Not started",
  "Blocked"
 ],
 "completion_rule": "A work package is Complete only when its PRD completion signal is satisfied. Code changes or risk containment alone are Partial.",
 "next_recommended_sequence": 8,
 "continuation_note": "LATEST (HEAD 3a47b94): OSS-002 public-surface wording is machine-enforced (check-source-available-wording.py in make check — 36 surfaces, reviewed negation allowlist, negative-verified with a planted claim; CHANGELOG wording lint approvals live in the fragment list). A runner flake in the locked-state Help test was root-caused and fixed: focus returns to #lock-help asynchronously after the overlay unmounts, so the instant evaluate could observe the body still focused; the abandoned assertion left the vault locked, cascading 33 timeouts (runner run with 39 passed/33 failed). The assertion now polls the same condition for up to 5s; comprehensive 72/72 twice from pristine stores locally and CI green on the fix commit. The recovery protocol below is unchanged and remains authoritative. ||| Slice thirty-three+ (owner-authorized live phase). COMPLETE: sequences 1 (live release audit executed; v0.0.1 corrected to prerelease; audit-releases.py --live PASS), 2 (registry + QA toolchain: DOC-003/004/005/006, QA-010/011/012/013/014, progress-consistency validator, test-plan generator + drift gate, license inventory, recovery-terminology lint), 3 (crypto fail-closed), 6 (GitHub-runner CI first-run green after five real fixes; two consecutive green runs). Sequence 11 CI halves (security-scan workflow: secrets, cargo-audit with documented unmaintained policy in .cargo/audit.toml, license inventory, actions-pinning lint, nightly seeded kernel fuzz). Browser acceptance fully runner-enforced: smoke, comprehensive (72/72, incl. all-locale sweep + UX-ONB-010 opt-in event inspection), a11y 19/19, keyboard 20/20, E2E journeys 6/6, fault suite 4/4, live token-gate 4/4, backup/wipe/restore drill (real TRUNCATE), README quickstart verbatim (against images built from the sources under test). Sequence 5 experimental lane fully implemented + E2E-002..007 journeys (wrapped Vault Key, per-vault verifier with migration, REC-001..007 incl. regeneration journey, encrypted portable backup + destructive restore, atomic password change incl. wrapper-only path, tombstone retention purge, adapter contract suite, reset matrix, windowed table + PERF-F-001 fixed: save ack p95 2811ms->32ms). PARTIAL (external, precisely recorded): seq 4 (independent crypto reviewer appointment — package tool + record kit ready, reviewer must be a named human via the owner's authenticated channel; AI review forbidden), seq 5/8 (ADR acceptance behind that review; E2E-008 needs a CA-certified public HTTPS hostname; published v1.0.0 app images predate DB-004 role-scoped startup — machine-proven stale; unlock = owner's next authorized publish-images dispatch, whose verify job now runs inspect-image --require amd64,arm64 over all 13 images + a pull-and-execute check), seq 7 (21 manual evidence records in docs/evidence/manual-evidence/ need named human operators; user study; E2E-001 clean-host install of the final signed package; E2E-010 manual a11y), seq 9 (Developer ID credentials — security find-identity returns 0 valid identities — plus macOS Intel/Windows AMD64/ARM64 hosts and E2E-009 hardware; local unsigned arm64 DMG evidence at release/evidence/native-preview/), seq 10 (OSI license choice Apache-2.0 vs AGPL-3.0 is an owner/legal decision; OSS-002 source-available wording machine-enforced meanwhile), seq 11 tail (review-gated assessment, reference-hardware PERF acceptance — local instrument exists at tools/release/bench-client.mjs with artifacts under release/evidence/perf/, soak/Beta/Stable decision). RECOVERY: work tree must stay clean on main, HEAD as pushed; all work is committed+pushed (no local-only state). Resume protocol: (1) re-read PRD.md, this ledger, CHANGELOG.md; (2) verify git status clean and CI+Security scan green on HEAD; (3) if any owner unlock input appeared (reviewer record filled in docs/evidence/independent-review/record.json, signing identity present, LICENSE changed from PolyForm, HTTPS domain/cert provided, or a publish-images run authorized), execute that sequence's final verification per its remaining_scope; (4) otherwise continue the automation gap audit: candidates already considered-and-done are listed above; next candidates to evaluate are any remaining QA/DOC/PRIV machine-checkable items and strengthening evidence tooling. Engineering notes: node via ~/.nvm/versions/node/v24.20.0/bin, cargo via ~/.cargo/bin with CARGO_TARGET_DIR=.build/cargo, port 3000 occupied (use VEYORA_WEB_PORT=3311 + VEYORA_DB_PASSWORD + VEYORA_API_AUTH=disabled + VEYORA_BUILD_COMMIT=e2e-local-check, VEYORA_WEB_URL=http://127.0.0.1:3311); compose down --volumes only with the compose env exported; web image must be rebuilt after apps/web changes; each browser suite needs a pristine store reset; run tools/release/ci-preflight.sh for the full local gate; make check now runs ten lints including actions-pinning, doc-commands, doc-ownership, test-plan drift, artifact hygiene, progress consistency, recovery terminology, browser selectors, foundation pins, compose topology.",
 "backlog": [
  {
   "sequence": 1,
   "work_package": "Freeze stable claims and correct release channel/status",
   "primary_ids": [
    "DOC-001",
    "DOC-002",
    "REL-011",
    "REL-012",
    "OSS-002"
   ],
   "status": "Complete",
   "completed_scope": [
    "Repository public surfaces describe the product as a source-available experimental preview; release workflows reject tags differing from the version authority and publish non-stable channels as prereleases.",
    "Repository public surfaces now describe the product as a source-available experimental preview.",
    "Release workflows reject tags that differ from the version authority and publish non-stable channels as prereleases.",
    "Reproducible release-metadata audit tool (tools/release/audit-releases.py): dry-run mode validates the repository side of the contract — version authority, the workflows' tag/version rejection gates, retired-tag registry, and the claim registry — and passes; --live mode (run by the owner with authenticated gh) audits every published release in one command: tag/authority match, no tag without a complete Release, no draft/prerelease-as-Latest, retired tags never stable, assets present. The blocker is unchanged and precise: live execution needs owner GitHub authorization; the tool makes that audit reproducible instead of a manual checklist.",
    "Owner unlock executed — live release audit complete and the published metadata corrected: audit-releases.py --live (run with owner-authenticated gh) audited the one published release (v0.0.1) against the version authority; it found the retired desktop-preview tag presented as a stable Latest release. Corrected on GitHub: v0.0.1 is now a prerelease with notes stating it is a retired unsigned preview tag that must not be a release baseline (matching version.json's historical_tag_exceptions), and the re-run audit passes (live audit PASS over 1 release against authority 1.0.0). The auditor's retired-key lookup was also corrected to the actual version.json schema (historical_tag_exceptions). Public download surfaces re-audited: the README already states no Stable download exists and the packages are unsigned/unnotarized preview artifacts, and the repo description carries no capability claim — both consistent with the corrected release state."
   ],
   "remaining_scope": [],
   "evidence": [
    "README.md",
    "CHANGELOG.md",
    ".github/workflows/publish-images.yml",
    ".github/workflows/desktop-release.yml",
    "release/version.json",
    "tools/release/audit-releases.py"
   ]
  },
  {
   "sequence": 2,
   "work_package": "Establish requirement/evidence registry and version authority",
   "primary_ids": [
    "DOC-001",
    "REPO-010",
    "REPO-011"
   ],
   "status": "Complete",
   "completed_scope": [
    "Added a single product-version and release-channel authority.",
    "Added a machine-readable feature/evidence registry with bounded public claims.",
    "Repository checks reject version, channel, feature, path, evidence, and kernel-asset drift.",
    "DOC-004/DOC-005 repository-side enforcement: tools/lint/check-doc-commands.py (in `make check`) parses every fenced shell block under bash -n, refuses prompt-polluted or non-copy-pasteable commands, requires explicit '# not-executable:' labeling for pseudocode, verifies referenced repo paths from both doc-relative and root contexts, and inventories runner prerequisites. Its first run caught a genuine executability defect — DEPLOYMENT-TLS.md's placeholder syntax `<generate-with: openssl rand -hex 32>` is a shell syntax error (and a redirect!) on copy-paste; the blocks now generate inline with \"$(openssl rand -hex 32)\" — and all 39 shell blocks across the shipped docs now parse. Full clean-runner execution remains CI-side (documented in the lint's inventory output). A new progress-consistency validator (check-progress-consistency.py, also in `make check`) mechanically enforces the goal's own records: PRD status markers must equal ledger statuses for sequences 1-11, Complete may never carry remaining_scope, non-Complete must state what is missing, every evidence path must exist, updated_at must be UTC ISO-8601, and CHANGELOG must keep an Unreleased section — currently PASS.",
    "QA-012 registry-driven release test plan: tools/release/generate-test-plan.py joins the feature/evidence registry against the PRD requirement table (priorities included) and the ledger's blockers, emitting release/test-plan.md with per-feature pass/fail/blocked verdicts — blocked dominates pass whenever any of a feature's requirements has a recorded blocker, so partial automation can never read as done (current honest output: 5 pass / 5 blocked / 0 fail). Producing it surfaced and fixed stale registry entries: recovery-key and master-password-change were still 'Protocol-only'/'Unsupported' and encrypted-portable-backup 'Planned' despite the implemented, browser-verified reality — all three now carry Experimental status with their real automated-evidence paths and known limitations.",
    "QA-013 machine-enforced (repository + product + suites): tools/lint/check-browser-selectors.py (in `make check`) classifies every text-coupled locator in tests/e2e/web — test-created data filters and copy-validation assertions are inventoried, while copy-driven business-flow selectors are violations. Its first run found 16 real violations; closing them added locale-independent identity to the product itself — toasts carry data-toast-key and every ceremony error host carries data-error-key (i18n key, invariant across locales) across the backup, recovery, regeneration, and password-change surfaces — and converted the suites to keyed selectors plus data-id row addressing: 0 violations remain, and the full browser regression re-passed (comprehensive 70/70 twice consecutively, smoke, a11y 19/19, keyboard 20/20, journeys 6/6, fault 4/4). QA-014 bootstrap verified from a clean copy of the current tree (29 MB, no caches, scratch git init): make check, check-codegen, check-web, 504-key locale parity, 108/108 web units, cargo build --locked --workspace, api 42/42, persistence 11/11, sqlite 8/8, desktop 10/10, security-kernel 98/98 across its crates, and git-clean after make clean — the only non-artifacts are the ignored .build trees.",
    "QA-010 repository half machine-enforced: tools/lint/check-artifact-hygiene.py (in `make check`) validates every evidence artifact under release/evidence/ — ISO-8601 UTC collection stamps, product-version/commit identity, a 180-day retention window with staleness refusal, and outright rejection of secret material (recovery-kit-shaped strings, bearer tokens, private keys, literal master passwords, test-suite credential literals). Its first run caught both perf artifacts missing version identity — now stamped with the product version and the pinned working-tree commit. DOC-006 is complete: docs/modes.md is the three-mode capability matrix (Desktop Local / Desktop Connected / Web Connected) with per-mode status, what differs in practice (storage, offline, diagnostics wording, recovery scoping), and honest limits — explicitly a view over release/features.json (registry wins on disagreement) and linked from the User Guide's offline section.",
    "DOC-003 machine-enforced: tools/lint/check-doc-ownership.py (in `make check`) requires every top-level document and docs/ page to declare an explicit Audience and Owner line (adr/evidence/reference/security/legal/brand/i18n live under their own governed hierarchies). Its first run caught 15 unstamped documents — all now stamped with real owners (documentation, release-engineering, QA, desktop maintainers, repository maintainers, architecture): 16 documents declare audience and owner, lint PASS.",
    "QA-012 drift gate + PRIV-003 documentation completion: release/test-plan.md is now machine-enforced against drift (tools/lint/check-test-plan.py in `make check` regenerates to a scratch file and fails on diff, mirroring the codegen gate; plan regenerated to current registry/ledger state — 5 pass / 5 blocked / 0 fail). The privacy inventory gains the first-success metric's full PRIV-003 documentation (purpose, default-off opt-in, no destination — nothing transmitted, exact content-free field set with the 100-event cap, deletion with site storage), matching the behavior the UX-ONB-010 inspection journey already asserts."
   ],
   "remaining_scope": [],
   "evidence": [
    "release/version.json",
    "release/features.json",
    "release/kernel-assets.json",
    "release/README.md",
    "tools/lint/check-repository.py",
    "tools/lint/check-doc-commands.py",
    "tools/lint/check-progress-consistency.py",
    "tools/release/generate-test-plan.py",
    "release/test-plan.md",
    "tools/lint/check-browser-selectors.py",
    "tools/lint/check-artifact-hygiene.py",
    "docs/modes.md",
    "tools/lint/check-doc-ownership.py",
    "tools/lint/check-test-plan.py"
   ]
  },
  {
   "sequence": 3,
   "work_package": "Make Web crypto fail closed and remove distributable DemoKernel",
   "primary_ids": [
    "SEC-CRYPTO-001",
    "SEC-CRYPTO-002",
    "SEC-CRYPTO-003",
    "QA-003",
    "QA-004"
   ],
   "status": "Complete",
   "completed_scope": [
    "Removed the Web pass-through demonstration crypto fallback.",
    "Browser startup now blocks Vault operations when binding/WASM fetch, digest, compile, instantiation, export, policy, or randomized self-tests fail.",
    "Security-sensitive kernel assets bypass stale HTTP caches and are pinned by digest.",
    "Missing, corrupt, impostor, policy-failure, self-test, and known-plaintext regression tests pass."
   ],
   "remaining_scope": [],
   "evidence": [
    "apps/web/src/core/kernel.js",
    "apps/web/src/config.js",
    "apps/web/src/wasm/veyora_kernel.js",
    "apps/web/src/wasm/veyora_kernel_bg.wasm",
    "apps/web/test/kernel.test.mjs",
    "apps/web/test/records.test.mjs",
    "apps/web/sw.js",
    "deploy/web/nginx.conf",
    "release/kernel-assets.json"
   ]
  },
  {
   "sequence": 4,
   "work_package": "Approve key/recovery/backup ADR and format",
   "primary_ids": [
    "REC-*",
    "BAK-*",
    "SEC-KEY-*",
    "MIG-001"
   ],
   "status": "Partial",
   "completed_scope": [
    "Added a proposed ADR defining a random stable Vault Key, independent password and recovery wrappers, scoped key confirmation, atomic password change and recovery ceremonies, separate staged Vault Key rotation, portable encrypted backup, and migration failure invariants.",
    "Added a machine-readable lifecycle contract, strict JSON schemas, and CDDL grammar for wrappers, key confirmation, and the portable backup envelope.",
    "Added a dependency-free checker and negative tests that bind the design to canonical project and backup vector hashes, generator source hashes, and backup ciphertext sidecar hashes without treating AI review as independent security approval.",
    "Independent-review materials kit: tools/release/prepare-review-package.py assembles the exact review scope (ADRs 0004/0005, machine contracts, kernel sources incl. the recovery-kit codec, client wrap/backup modules, lifecycle checkers, projection tests — 19 files) into a flat directory with a commit-pinned SHA-256 manifest (built and verified: commit c3fe009, manifest 4be36109…), and docs/evidence/independent-review/ holds the appointment + review-record structure (independence statement, findings with severity/disposition, signature binding to the package hashes, Pending until a qualified human performs it). The blocker is unchanged: the owner must appoint the reviewer through their authenticated channel; when they do, the package and record make the review and its evidence mechanically checkable."
   ],
   "remaining_scope": [
    "Appoint and register a qualified independent cryptography reviewer, resolve review findings, and add signed review records that bind the ADR, contracts, CDDL, and relied-on vector corpora.",
    "Obtain qualified review for the relied-on project and native-backup vector rows, including a disposition for the blocked upstream fixed-decrypt capture.",
    "Update the threat model and compatibility contract from the approved decision, then change the ADR and lifecycle contract to Accepted only after the review checker can verify the recorded approval."
   ],
   "evidence": [
    "docs/adr/0004-key-recovery-backup-lifecycle.md",
    "contracts/protocol/key-lifecycle-v1.json",
    "contracts/protocol/key-lifecycle-v1.schema.json",
    "contracts/protocol/key-lifecycle-review-v1.schema.json",
    "contracts/protocol/approved-evidence-reviewers-v1.schema.json",
    "contracts/cddl/key-lifecycle-v1.cddl",
    "tools/lint/check-key-lifecycle.py",
    "tests/contracts/test_key_lifecycle.py",
    "packages/security-kernel/vectors/v1.json",
    "packages/security-kernel/vectors/backup-v1.json",
    "tools/release/prepare-review-package.py",
    "docs/evidence/independent-review/README.md",
    "docs/evidence/independent-review/record.json"
   ]
  },
  {
   "sequence": 5,
   "work_package": "Implement scoped schema, verifier, atomic password change, recovery, backup/restore, batch/import",
   "primary_ids": [
    "DATA-*",
    "REC-*",
    "BAK-*",
    "IMP-*",
    "SEC-KEY-*"
   ],
   "status": "Partial",
   "completed_scope": [
    "Recovery and Master Password change controls are hidden, and the capability registry marks these flows unsupported, preventing unsafe claims while designs are absent.",
    "DATA-001/002/004 (experimental scope per the sequence-4 gate; no supported-capability claim): record storage identity is now (vault_id, record_id) everywhere — the persistence port scopes get/list/list_bodies/tombstone/purge by vault, the InMemoryStore keys rows by the pair, PostgreSQL migration 0002 replaces the single-column primary key with the composite pair (existing single-vault rows unchanged), the SQLite adapter gains the same scoped keys plus an ordered idempotent migration runner (0002) and a vault_ids() helper for the single-vault desktop shell, and every SQL statement filters by vault. The API requires a declared vault scope (?vault=) on every read/listing/delete/purge (missing scope = PM-API-BAD-QUERY), validates the envelope's non-empty vault_id on writes alongside the existing route/body identity check (DATA-004), and the web client + Connect probes + smoke script carry the scope (the client-side salt filter remains as defense in depth). New blocking tests prove the E2E-005 collision scope: the same record ID written for two vaults stays two independent rows, each listing sees only its own, and a cross-scope read is 404 (API unit test + live PostgreSQL test incl. migration on a real store); a missing-scope request is a stable-code rejection. Verified: workspace fmt/clippy -D warnings/test (0 failures), api 38 tests, persistence 10, sqlite 4, postgres 6 unit + live ignored round-trip/CAS + least-privilege tests against the real compose stack, make check + check-codegen green, and on rebuilt images the full browser regression on pristine stores (comprehensive 60/60, accessibility 18/18, keyboard 20/20) plus API/header/browser smokes. Status stays Partial: the declared (not authenticated) vault scope is interim until ADR 0005's pairing principals, and DATA-003/005/006/007/008/009/010 remain.",
    "DATA-007/API-010/IMP-001 server scope (experimental lane, gate intact): OpaqueStore gained a transactional put_batch — PostgreSQL and SQLite execute every row in ONE transaction (any invalid or conflicting row rolls the whole batch back), the InMemoryStore plans atomically under a single lock, and the fallback trait default exists only for test doubles. /records/batch is now all-or-nothing: all records must declare one vault scope (mixed-scope bodies are PM-API-BAD-BODY), a conflict returns the PM-STORE-CONFLICT envelope with nothing applied, and the success response carries exact counts (requested/committed plus per-row revisions — requested is never treated as a success count). Blocking tests: unit rollback+atomic-commit for the in-memory and SQLite adapters, API tests proving a conflicting row leaves the store untouched (fresh row absent, seeded revision unchanged) and mixed-scope rejection, the web import client updated to the exact-count contract (all-or-nothing; failure via the error envelope), live PostgreSQL proof inside the ignored round-trip test (one stale CAS row rolls back, committed rows untouched), and api.md documents the atomic contract. Verified end-to-end: workspace fmt/clippy -D warnings/test (0 failures), api 41 tests, persistence 11, sqlite 5, postgres unit + all three ignored live tests green on the rebuilt role-initialized stack, make check + check-codegen + repository check (422 files) green, and the SECOND consecutive pristine full-browser regression: comprehensive 60/60, accessibility 18/18, keyboard 20/20, API/header/browser smokes all passing (two consecutive rounds, exit 0).",
    "DATA-005/006 (experimental lane): listings are bounded and paginated — the port and all three adapters take explicit (limit, offset), the API accepts ?limit/?offset with a strict maximum (default 500, hard cap 5000; oversized or zero limits are PM-API-BAD-QUERY, never silently honored), the ?embed=bodies hydration path carries its own strict 500-row maximum (larger vaults answer the stable error and the client pages summaries), and every response exposes an x-truncated marker when another page may exist; the web client gained apiFetchAllPages so unlock, re-key, verification, and counting hydrate every page of large vaults. DATA-005: compare-and-set conflicts now answer 409 with the server-authoritative current revision in the typed parameters map (parameters.current_revision, read fresh from storage on the conflict path; vanished rows keep the bare code) — ErrorDetail gained the safe-parameters field per PRD 25.4. Verified: new blocking API tests (pagination bounds/marker + oversized rejection; stale-CAS payload carrying current_revision), live-stack proof (x-truncated:true observed; a real stale PUT answered PM-STORE-CONFLICT with current_revision:2), all three live PostgreSQL ignored tests green, workspace fmt/clippy -D warnings/test with 0 failures, web unit 85/85, and after the contract change the full pristine browser regression passed again (comprehensive 60/60, accessibility 18/18, keyboard 20/20, API/header/browser smokes).",
    "DATA-003 (experimental scope): the password verifier's record identity is now scoped per vault — the id is derived from the vault salt (veyora-verifier-v1-<salt>), so no two vaults on one store share a verifier record id, and new writes never use the global fixed id. Legacy vaults that still hold the pre-scoping id unlock against it and migrate on sight (scoped verifier written first, legacy tombstoned after; a crash between the two is safe and retries next unlock; a 409 on the write means another device migrated). Proven by four new blocking unit tests: scoped ensureVerifier per salt, two-vault independent verification with an empty vault still rejecting a wrong password, the legacy migration contract (scoped PUT + legacy DELETE with expected revision), and no rewrites for already-scoped vaults (89/89 web unit tests; comprehensive browser suite 64/64 twice consecutively plus smoke, all from pristine stores).",
    "DATA-008 (experimental scope): tombstones now carry a deletion stamp (tombstoned_at, epoch seconds, NULL for live rows) set on soft-delete and cleared on restore, with migration 0003 backfilling pre-migration tombstones at the migration epoch so retention never fires early. Purge is one cutoff-scoped operation shared by every caller — purge_tombstoned_before(vault, before_epoch) — with the API's POST /vault/purge purging at the current epoch and the worker enforcing the operator retention policy (VEYORA_TRASH_RETENTION_DAYS, default 30 per DEC-009/ITEM-009; 0 disables; the cutoff saturates at zero so a skewed clock deletes nothing extra). Live evidence: the worker's sweep logs purged counts on the e2e stack, live PostgreSQL tests backdate a stamp through the database itself and prove only the expired tombstone is removed, and the pristine browser regression is green (comprehensive 64/64 twice consecutively, smoke, accessibility 19/19, keyboard 20/20) after rebuilding the migrator image with 0003.",
    "DATA-010 (experimental scope): a shared behavioral contract suite (backend_persistence::contract) runs identical assertions against every adapter — round trips, server-assigned revisions, compare-and-set conflicts, (vault_id, record_id) isolation, bounded paging, all-or-nothing batches, tombstone stamp/restore lifecycle, and cutoff-boundary purge semantics — and SQLite (7/7) and live PostgreSQL (plus the in-memory reference) all pass it. Adapter harness fixes that fell out: PostgresStore::migrate() now reaches the real current schema (the composite primary key was previously skipped on the dev path, which built a wrong database), the ignored live tests are serialized through a shared guard because the migration files take table locks, and each live test cleans only its own vault scope instead of wiping the shared database.",
    "DATA-009 (experimental/web scope): destructive Reset is a labeled matrix in Settings → Data — four distinct actions, each deleting exactly one scope and naming its consequence before the two-step confirm: device preferences (theme, language, retention, checklist, sync timestamp, sort/nav — never vault data), the saved service connection (URL/token overrides), this vault's device copy (queued offline writes plus the vault record, with the confirmation stating the service copy becomes unreachable), and delete-everywhere, which deletes the service scope FIRST (every live row tombstoned including the reserved verifier, then one scoped purge) and removes nothing local on failure — an unreachable remote orphan can never be created by accident. Five blocking unit tests pin the scope boundaries and the failure guarantee; two new browser journeys run against the real service, including a post-reset audit proving the vault scope answers an empty records listing (comprehensive suite 66/66 twice consecutively; smoke, accessibility 19/19, keyboard 20/20; 448-key parity across all ten catalogs).",
    "Atomic Master Password change (experimental scope, storage + client core): the previous flow re-sealed every row under the OLD salt and then flipped the device salt, silently orphaning the entire service scope. It is now a server-side atomic vault rekey — a new OpaqueStore operation (rekey_vault) inserts every re-sealed row under the fresh salt and deletes the old scope in ONE transaction in all three adapters (a target collision or any failure rolls everything back, proven in the shared adapter contract suite and a live PostgreSQL run), exposed as POST /vault/rekey with strict mixed-scope rejection (api 42/42 including a full move-and-collision test). The client changeMasterPassword now refuses while offline writes are pending, verifies the current password, snapshots every row (verifier included, whose identity moves to the new salt's derived id), seals target rows, records a pending-rekey marker BEFORE the request and clears it after adoption — resolvePendingRekey lets the next unlock adopt a committed target scope or fall back cleanly, so the crash window between server commit and device adoption is recoverable (20/20 records unit tests, 96/96 web units, comprehensive browser suite 66/66 twice consecutively plus smoke/accessibility/keyboard after rebuilding the api and web images). The Settings UI ceremony for the change remains (the function is correct, tested, and callable; no UI caller yet).",
    "Master Password change UI ceremony (web scope): Settings → Security now offers the change with the same composition-free rules as vault creation (15-char floor, local common-material screening, confirm field), the current password verified by the atomic rekey itself, and every failure class surfaced inline without discarding typed input — a wrong current password states plainly that nothing changed (including correct handling of the kernel's raw PM-KERNEL string rejections), and pending offline writes are refused with a sync-first instruction. The unlock path now resolves an interrupted change (resolvePendingRekey adopts a committed target scope or falls back), closing the crash-window protocol. A blocking browser journey proves the whole chain against the live service: wrong password refused, UI rekey preserving every entry and Trash state, lock, and unlock with the NEW password (comprehensive suite 67/67 twice consecutively; 456-key locale parity × 10 catalogs).",
    "Encrypted portable backup + restore, web scope (EXP-001): Settings → Data now offers Create encrypted backup and Restore backup as first-class actions. The envelope (`veyora-backup` v1) is portable — sealed under the master password with a salt generated per backup, independent of the live vault salt — and every entry is sealed on its own, respecting the kernel's per-record plaintext limit (the initial single-blob design tripped PM-KERNEL-LIMIT-EXCEEDED and was replaced; each stored entry already satisfies the limit by construction). Creation re-authenticates through the real verifier, reports wrong passwords inline with 'nothing was created', downloads an all-or-nothing file, and the completion stage names the count and that only the master password opens it. Restore decrypts the picked file, verifies the concatenated-plaintext digest, advertises the count before asking for the password, and merges through the atomic batch endpoint with id de-duplication — existing entries are never overwritten (a restored colliding id imports suffixed). Five unit tests cover round trip, wrong password, tampering, foreign shapes, and per-backup salt independence; a blocking browser journey exercises the whole chain against the live service including a post-restore server-side row audit (comprehensive suite 68/68 twice consecutively; smoke, accessibility 19/19, keyboard 20/20; 473-key parity × 10 catalogs).",
    "Browser acceptance journeys for the locally runnable E2E set (new blocking suite tests/e2e/web/test-browser-e2e.mjs, `make test-browser-e2e`): E2E-002 empty vault refuses wrong passwords with byte-identical localStorage digests and the correct password still unlocking; E2E-005 two vaults on two isolated browser devices with intentionally colliding item ids stay isolated across list/read/delete (a server-side scoped audit proves each scope answers exactly its own row, and deleting in one vault never touches the other's colliding row); E2E-006 malformed-middle-row and duplicate-id imports commit nothing while a valid multirow file commits exactly its rows (this also produced the first real browser-driven CSV import coverage — the previous suite only asserted the button existed); E2E-007 backup restore into an emptied destination (delete-everywhere → fresh vault → restore → full comparison + post-restore CRUD) with truncated/wrong-version/wrong-password inputs refused without destination mutation. E2E-004's failure-injection leg runs in the fault suite: 100 items imported through the real path, the store killed mid-change, the failure surfaced inline, and after recovery every record plus the OLD password remain usable. All green twice consecutively (journeys 4/4 ×2, fault 4/4 ×2, comprehensive 68/68 ×2, smoke/a11y/keyboard). The journeys exposed and fixed a real product defect: vault.reset() left the destroyed vault's decrypted entries in memory, which then rendered into a vault created later on the same page — reset now zeroes the in-memory list.",
    "Recovery Key architecture (REC-001..005, web scope) and E2E-003: the vault now uses a wrapped Vault Key — 32 random bytes generated at creation from which every record key derives — with TWO wrappers: the password wrapper (VK sealed under deriveRootKey(master password, vault scope), stored as a reserved record in the vault scope) and the recovery wrapper (the same VK sealed under deriveRootKey(Recovery Key, kit scope), where the kit scope is the 16-byte SHA-256 salt of the 71-character kit — so a fresh device finds the wrapper from the kit alone, and the payload carries the vault scope back). Legacy vaults migrate transparently on their next unlock (the old password-derived root key IS the Vault Key, so migration writes only the wrappers — zero record rewrites, proven by a blocking unit test). Password change on a wrapped vault replaces ONLY the password wrapper (compare-and-set, one retry on lost-create races; the Recovery Key keeps working unchanged) — the heavier server-side rekey remains for scope rotation. Recovery: the Welcome and unlock screens offer 'Recover with Recovery Key'; the kernel codec validates the kit (checksum, no case correction), the wrapper is fetched at the kit-derived scope, the recovered VK must decrypt the real vault's verifier BEFORE adoption, and only then is a new password wrapper written. E2E-003 passes as a blocking browser journey on a fully wiped device: malformed kit refused inline, real kit recovers every item, the OLD password stops unlocking, the new one works, and post-recovery CRUD functions. 107/107 web units (wrap round trip, wrong password, foreign kit, migration, recovery with CAS-verified wrapper replacement); full browser regression green (comprehensive 68/68 twice, smoke, a11y 19/19, keyboard 20/20, journeys 5/5, fault 4/4); 478-key locale parity × 10 catalogs.",
    "REC-006 and REC-007 complete (web scope): Settings → Security offers Regenerate Recovery Key — re-authentication through the password wrapper, a new kit written FIRST at its own derived scope, the old kit's wrapper tombstoned compare-and-set (and recovery treats a tombstoned wrapper as revoked — the row may keep its ciphertext but never opens again), a verified encrypted backup created AND opened back before success is reported, the new kit displayed with storage guidance, and the revocation consequence stated. A blocking browser journey proves the whole chain on wiped state: wrong password refused, old kit dead, new kit recovers the same vault (journeys 6/6 twice; 108/108 web units; comprehensive 68/68 twice; smoke, a11y 19/19, keyboard 20/20, fault 4/4). REC-007 is machine-enforced: tools/lint/check-recovery-terminology.py (wired into `make check`) refuses 'recovery code'/'recovery password'/'backup password' phrasings and any 2FA-recovery conflation across all ten catalogs and the shipped docs."
   ],
   "remaining_scope": [
    "Advance the experimental scoped-schema/verifier/recovery lane to Accepted once sequence 4 delivers the qualified independent cryptography review of ADR 0005 (the implementation, adapter contract suite, and E2E-002..007 evidence are in place and re-runnable)."
   ],
   "evidence": [
    "apps/web/src/views/entry-flow.js",
    "apps/web/src/views/drawer.js",
    "release/features.json",
    "packages/storage/persistence/src/lib.rs",
    "packages/storage/postgres/src/lib.rs",
    "packages/storage/postgres/migrations/0002_records_vault_scope.sql",
    "packages/storage/sqlite/src/lib.rs",
    "packages/storage/sqlite/migrations/0002_records_vault_scope.sql",
    "services/api/src/lib.rs",
    "services/api/tests/e2e_integration.rs",
    "apps/web/src/core/records.js",
    "apps/web/src/views/connect.js",
    "tests/smoke/api.sh",
    "docs/reference/api.md",
    "packages/storage/sqlite/src/lib.rs",
    "apps/web/test/records.test.mjs",
    "docs/browser-test-inventory.md",
    "services/worker/src/lib.rs",
    "services/worker/src/main.rs",
    "packages/storage/postgres/migrations/0003_records_tombstone_retention.sql",
    "deploy/compose/compose.yaml",
    "docs/runbook.md",
    "apps/web/src/core/reset.js",
    "apps/web/test/reset.test.mjs",
    "apps/web/src/core/pending-queue.js",
    "apps/web/src/core/backup.js",
    "apps/web/src/views/backup.js",
    "apps/web/test/backup.test.mjs",
    "tests/e2e/web/test-browser-e2e.mjs",
    "apps/web/src/core/vault.js",
    "apps/web/src/core/recovery.js",
    "apps/web/test/recovery.test.mjs",
    "tools/lint/check-recovery-terminology.py"
   ]
  },
  {
   "sequence": 6,
   "work_package": "Normalize repository ownership, language, workspaces, codegen, build paths, real CI",
   "primary_ids": [
    "LANG-*",
    "REPO-*",
    "QA-001",
    "QA-002"
   ],
   "status": "Complete",
   "completed_scope": [
    "Normalized product versions through release/version.json.",
    "Expanded English-source and repository-integrity enforcement.",
    "Corrected several public claims and release workflow gates.",
    "Added repository-layout.toml as the layout authority and enforced it in the repository check: unapproved tracked root entries and file types, non-ASCII/space/case-colliding paths, tracked runtime state, unapproved working-tree root artifacts (including an empty password/ directory), and experiments referenced by shipping CI or the Makefile are now rejected (REPO-001/002/003/016/017).",
    "Moved quality tooling by responsibility with history-preserving renames: lint checks to tools/lint, browser E2E to tests/e2e/web, the API smoke test to tests/smoke/api.sh, the backup/restore drill to tests/integration/backup-restore.sh, deployment tooling to deploy/tools, and container publishing to tools/release; all documentation, workflow, Makefile, and registry references were updated.",
    "Archived the m0-desktop and m0-android capability spikes under experiments/ so they can no longer satisfy shipping CI; the Makefile and CI now check the shipping desktop application instead of the obsolete spike (QA-002, REPO-003).",
    "Removed the shell success bypass from the comprehensive browser E2E in CI, making that suite blocking (QA-001); suites run against isolated pristine stores via a data-volume reset (containment for the known DATA-003 global verifier id, not the fix).",
    "Added CODEOWNERS with specialist-review areas (REPO-015).",
    "Completed the PRD section 12.3 ownership migration with history-preserving renames: frontend/web to apps/web, frontend/desktop to apps/desktop, backend/services/* to services/*, backend/crates/{persistence,postgres,sqlite} to packages/storage/*, backend/crates/config to packages/config, backend/crates/contracts-generated to packages/contracts-rust, backend/tooling to tools/codegen/backend, the backend aggregate test crate to tests/architecture, backend/Dockerfile.* and docker/* to deploy/containers|compose|gateway|web (REPO-004/005). All Makefile, CI, Dockerfile, Compose, checker, registry, and documentation references were updated and the legacy backend/, docker/, and frontend/ roots no longer exist.",
    "Established the root non-kernel Rust workspace (Cargo.toml, Cargo.lock, rust-toolchain.toml at the root) with services, packages, tests/architecture, and the shipping desktop crate apps/desktop/src-tauri as members, removing the desktop ../../../backend product-boundary crossing (REPO-005); the security kernel intentionally keeps its independent workspace.",
    "Established the root Node workspace with one root package.json/package-lock.json covering apps/desktop and tests/e2e/web; npm ci now installs the whole workspace and the stale per-package lockfile and pnpm leftovers were removed (REPO-009).",
    "Verified locally on macOS ARM: repository check PASS, root workspace fmt/clippy/test (including the rewritten architecture conformance suite), security-kernel fmt/clippy/test, web unit tests 52/52, codegen unit tests 12/12, Compose config validation plus migrator/web/gateway image builds from the new Dockerfiles, API smoke test, browser smoke, the blocking comprehensive browser E2E 17/17, panic/secret log checks, and git diff --check.",
    "Renamed services/sandbox to services/validator with history-preserving moves (crate name, Dockerfile/image veyora-validator, publish and CI paths, Makefile target, docs) and renamed the capability key, setting ids, image/repository lists, and ServiceNameV1::Sandbox across contracts; packages/config/src/generated.rs was regenerated through tools/codegen/backend/generate_backend_projection.py from the updated inputs and its pinned capabilities digest was updated with the architecture test that asserts it (REPO-004 map completion).",
    "Routed transient Rust output under the ignored .build tree: the Makefile exports CARGO_TARGET_DIR=.build/cargo for the root workspace and uses .build/kernel for the security-kernel workspace, with all debug-binary paths updated; added make clean (removes .build), make clean-all (additionally removes node_modules, the legacy root/security-kernel target directories, and the 5.7 GB apps/desktop/src-tauri/target), and make doctor via tools/lint/doctor.sh, which is shellcheck-listed in CI. clean-all and doctor were executed and returned the checkout to a bounded ~52 MB tracked state (REPO-012/013 plus the doctor check).",
    "Moved security-kernel/ to packages/security-kernel with history-preserving renames while keeping its independent Cargo workspace, lockfile, and review boundary (REPO-004 completion): Makefile, CI, Dependabot, CODEOWNERS, gitleaks, doctor, repository-layout.toml (now a nested packages/security-kernel entry with longest-prefix matching in the repository checker), check-repository version consumers, contracts, provenance records, feature registry, experiment evidence, and documentation paths were updated; vector generator source refs and fixture_sha256 digests were recomputed and the lifecycle checker, contract tests, and kernel fmt/clippy/test suite pass; the kernel FFI harness and build-ffi.sh repo-root resolution were adjusted for the deeper nesting; a pre-existing YAML indentation bug in the CI rust job was fixed.",
    "Single-sourced tracked generated artifacts (REPO-006/007/008): tools/codegen/manifest.json declares generator, inputs, outputs, consumers, digests, regenerate command, and check mode for the registry projection, Rust config projection, both contract bindings, and the WASM assets; tools/codegen/check_codegen.py (run by the new make check-codegen target and the CI rust job) fails on missing generators/outputs, digest drift, or a failing deterministic check; the backend Rust projection check regenerates into .build, applies rustfmt, and byte-compares. make codegen regenerates everything and refreshes manifest digests.",
    "Added the missing producers: tools/codegen/backend/generate_registry_projection.py regenerates packages/config/registry.generated.json from contracts/registry/settings.json (owner==backend filter, reproducible source/projection digests, unit tests), and tools/codegen/contracts/generate_bindings.py plus contracts_types_template.rs regenerate both Rust contract bindings from one template with a reproducible CONTRACT_SOURCE_DIGEST over the whole contracts inventory and per-output projection digests; digests were re-derived and the architecture-test digest assertion updated.",
    "Enforced language rules: shipped-locale key parity is release-blocking in check-locales (LANG-005, all 10 locales currently at 234-key parity), the LANG-002 path-component case lint was added to check-repository with an explicit convention allowlist, and the generic browser E2E and root README were removed from the CJK allowlist after verifying they are English-only (LANG-001/004; the API error catalog remains the only source allowlist entry pending LANG-003).",
    "Single-sourced the WASM generation story (REPO-007): make build-wasm is documented as the one command and apps/web/src/wasm the one destination in the web and kernel READMEs, with digest pinning via release/kernel-assets.json; the stale deployment/web/wasm/ path claim was removed.",
    "Fixed a post-migration regression: the backend architecture suite no longer scans packages/security-kernel as backend source (it keeps its own workspace and review boundary), restoring the suite to green.",
    "Implemented LANG-003: services/api/src/error_catalog.rs now holds only stable ASCII codes with fixed English messages, the Accept-Language negotiation middleware (and its Content-Language/Vary rewriting) was removed, and API contract tests assert that Accept-Language cannot change the message; the web client renders localized API errors from new apiError.* keys (11 keys, 245-key parity) in all ten locale catalogs through apiErrorMessage(), wired into the dashboard, drawer, modals, and unlock-failure paths; the services/api/src/error_catalog.rs CJK allowlist entry was removed from the repository checker, which now permits non-English text only in locale catalogs and i18n docs/registries.",
    "Ran the fresh-clone G1 gate on the local Mac: cloned the repository into a scratch checkout, synced the migrated working tree (the migration is not yet committed), ran npm ci (0 vulnerabilities), python3 tools/lint/check-repository.py PASS over 377 tracked files, git diff --check and git diff --cached --check clean, make check-codegen PASS (4 artifacts) from a cold .build, web/locale/codegen/contract suites (web unit tests, locale parity, 17 codegen unit tests, 7 contract tests), make test-backend (fmt+clippy+test over the root workspace) and the security-kernel cargo test suite, then make clean-all, which left zero untracked git entries and a bounded ~53 MB checkout with no stray root artifacts. The gate surfaced real drift that was fixed in the source of truth: the tools/codegen files were untracked so the layout authority had never seen them (repository-layout.toml now allows the *.rs contract-binding template under tools), trailing whitespace in .github/workflows/ci.yml and blank-line-at-EOF defects in contracts/cddl/key-lifecycle-v1.cddl and docs/adr/0004-key-recovery-backup-lifecycle.md were removed, and make codegen regenerated both contract bindings plus the config projection and refreshed manifest digests; all previously untracked sequence-6 files are now staged so the repository check validates the full migrated tree.",
    "Replaced the CI compose job's ad-hoc browser harness: the two browser suites no longer npm init/npm install playwright into /tmp; CI runs npm ci from the root Node workspace (playwright-core is a workspace dev dependency of tests/e2e/web) and executes the checked-in tests/e2e/web scripts from the repository root, relying on the suites' existing bundled-chromium then chrome/msedge-channel launch fallbacks (GitHub ubuntu runners preinstall Chrome and Edge). Verified locally that npm ci installs the workspace, that the E2E scripts resolve playwright-core and launch a browser from the repository root, and that the edited workflow is valid YAML; the first GitHub-runner execution is still pending the owner's push.",
    "Full-tree verification after the sequence-8 hardening window: every quality gate re-run green on the current tree — make check (repository PASS 420 files, manual-evidence, pairing/foundation-pins/compose-topology gates, 18 contract tests), web integrity/37 modules, locale parity 421 keys x10 catalogs, web unit 85/85, make check-codegen PASS (the new pairing contract files had changed the contracts-inventory digest; bindings regenerated and manifest digests refreshed, and the architecture suite's dependency-edge map and forbidden-literal scan were updated for the shared UTC logging helper — a duplicate key in the expected-edges map was also removed), workspace fmt/clippy -D warnings, desktop 9/9, security-kernel workspace 104 tests, architecture suite 12/12, and the three blocking browser suites each on a pristine store reset (comprehensive 60/60, accessibility 18/18, keyboard 20/20) plus the API smoke — confirming the hardening window introduced no regression anywhere in the tree.",
    "DATA-003 is fixed in the working tree (not just contained): the verifier record id is derived per vault (veyora-verifier-v1-<salt>) with legacy migration on unlock, proven by blocking unit tests and pristine browser regressions — the sequence-5 experimental lane carries the detail; the CI store reset remains as defense in depth.",
    "CI first-run preflight (tools/release/ci-preflight.sh) executed end-to-end green locally: toolchain versions, make check (all repository gates incl. manual-evidence, pairing, key-lifecycle, recovery terminology), check-codegen, check-web, locale parity, web units, workspace fmt/clippy -D warnings, the security-kernel workspace's own tests, api tests, desktop shell tests, contracts tests, and codegen tooling — every repository-side check the migrated workflows run, in CI order. A clippy -D warnings failure it exposed in the desktop test module was fixed (unused import after the INST-006 path tests). The remaining gap is exactly what the tool names: runner-only verification (owner push) — migrated jobs on real GitHub runners, published-image digest checks on the runner network, and cold-cache desktop toolchain behavior.",
    "GitHub-runner CI first-run verification COMPLETE (run 34061368314, all six jobs green: Repository integrity, Compose integration incl. comprehensive+a11y+keyboard browser suites against the live stack with pristine resets, Desktop shell, Rust workspace, Security kernel, WebAssembly build). Reaching green required five real fixes surfaced only on runners: (1) cargo fmt has no -p flag — the desktop job's fmt step crashed; (2) the Rust workspace job lacked the Linux WebView build dependencies now that the workspace includes the desktop crate (glib-sys failed); (3) the kernel WASM runtime test is CommonJS under the root workspace's ESM type — renamed .cjs with workflow/registry references and a layout-manifest update; (4) the between-suite stack resets dropped VEYORA_BUILD_COMMIT so the re-rendered web config served 'unknown' — all three reset steps now re-pass the stamp (and the stale DATA-003 workaround comment is corrected to hygiene rationale); also hadolint flags must ride the container command (the image has CMD, not ENTRYPOINT) and the e2e fixture passwords are gitleaks-allowlisted as inert literals; (5) the architecture test's expected workspace map predates the restructure (worker's postgres edge, apps/desktop allowlisting). Every fix was committed and pushed; the final run is green end-to-end on ubuntu-latest runners."
   ],
   "remaining_scope": [],
   "evidence": [
    "release/version.json",
    "tools/lint/check-repository.py",
    "repository-layout.toml",
    ".github/workflows/ci.yml",
    ".github/CODEOWNERS",
    "Makefile",
    "Cargo.toml",
    "package.json",
    "apps/web",
    "apps/desktop",
    "services",
    "packages",
    "deploy/compose/compose.yaml",
    "services/validator",
    "deploy/containers/validator/Dockerfile",
    "tools/lint/doctor.sh",
    "tests/architecture/tests/architecture.rs",
    "tests/e2e/web/test-browser-full.mjs",
    "tests/smoke/api.sh",
    "packages/security-kernel",
    "tools/codegen/manifest.json",
    "tools/codegen/check_codegen.py",
    "tools/codegen/contracts/generate_bindings.py",
    "tools/codegen/backend/generate_registry_projection.py",
    "apps/web/tools/check-locales.mjs",
    "services/api/src/error_catalog.rs",
    "apps/web/src/i18n/index.js",
    "apps/web/locales/en.json",
    "apps/web/test/i18n.test.mjs",
    "apps/web/src/views/drawer.js",
    "apps/web/src/views/modals.js",
    "apps/web/src/views/dashboard.js",
    "services/api/src/lib.rs",
    "tools/release/ci-preflight.sh"
   ]
  },
  {
   "sequence": 7,
   "work_package": "Rebuild Welcome/core UX/errors/settings/help/accessibility",
   "primary_ids": [
    "UX-*",
    "ITEM-*",
    "NAV-*",
    "EXP-*",
    "ACC-*"
   ],
   "status": "Partial",
   "completed_scope": [
    "Made Trash reachable and added record restoration.",
    "Added focused-grid keyboard navigation and browser coverage.",
    "Removed misleading recovery/password-change controls and improved creation failure handling.",
    "NAV-002: the search field carries a scope-naming placeholder and aria-label that follows the active view (Search all items / favorites / type / trash).",
    "NAV-003: active scope and text filters render as individually removable chips above the table, each with an accessible remove label.",
    "NAV-004: the truly-empty vault state says Your vault is empty and offers Add your first login plus Import items, the latter wired to the same CSV import flow as Settings.",
    "NAV-005: the no-results state names the query and scope and offers Clear search and Search all items escapes.",
    "ITEM-008 (partial): delete-to-trash now shows a toast with an Undo action for both the drawer and row-menu paths (restores the tombstone); toasts became pointer-interactive while overlays are open. Permanent-deletion confirmation naming counts remains with the purge flow.",
    "Locale coverage for all new UI strings across all ten catalogs at 255-key parity, and browser E2E assertions for every new behavior (comprehensive suite 21/21).",
    "UX-ONB-001/004 (implementation): the Welcome screen states what Veyora does, what a Vault is, and that the current mode is a connected vault before requesting a password; the create form explains storage, password rules, unavailable recovery, and that encrypted backup is unavailable and plaintext CSV export is not a backup.",
    "UX-ONB-005/006 (partial): Master Password creation enforces a 15-character composition-free floor; browser coverage proves inputs longer than 64 characters preserve Unicode and spaces and that Show/Hide is keyboard accessible. A new local common-password screen (data/blocklist.js) rejects passwords built from an embedded list of widely breached material — case-insensitive, whitespace-folding, purely on-device with no network or hash disclosure — explains the check in the pre-commit disclosure, and is covered by unit tests and a browser assertion. Autofill/platform manual evidence and threat-model approval remain.",
    "ITEM-007: successful create and edit saves present a status panel naming the item with Back to vault, Copy secret (when present), and Add another item next actions; Back closes both the modal and any underlying detail drawer.",
    "ITEM-006/QA-015 (web implementation): edits are staged in a candidate object and copied into session state only after the encrypted API write succeeds; a rejected write leaves the last saved secret in memory and keeps the form open. ITEM-006 status reporting (web implementation): a failed save renders a dedicated role=alert status banner in the form that names the failure class and states that the vault was not changed — network/service failures offer Retry save, which re-runs the same idempotent write (create reuses the derived id; edits reuse the same compare-and-set revision, so a retry can never duplicate the item); a 409 conflict states the item changed on another device, preserves the edits in the form, and offers Reload latest version, which refetches and re-opens the form on the server's version; other rejections surface the localized API error with the same retry path. Successful saves now report Synchronized with the service — revision N in the post-save panel.",
    "ITEM-009 (partial): Trash retention is a validated persisted preference with a 30-day default and 7/30/90/365-day options; the UI explicitly says automatic deletion is unavailable because scoped server-side purge enforcement remains.",
    "HELP-001/002 (automated Web/shared-Tauri UI scope): task-routed Help is reachable from Welcome, Locked, and Unlocked states, covers get started, recovery, encrypted backup, restore, connect, troubleshoot, and private security reporting, exposes only safe contextual actions, and restores focus on close.",
    "UX-ONB-008/009 (automated Web scope): a dismissible Start here checklist guides first login/import, copy/reveal, lock, and — honestly marked as unavailable in this preview — recovery verification and encrypted backup; progress persists in localStorage, the card auto-hides on completion, and it reopens from Help.",
    "NAV-006: search matches a single documented field set (name, username, website, service, host, notes, secret, item type) via data/search.js with unit coverage of the field boundary, is documented in the shortcuts dialog and user guide, and a browser assertion proves typing in search triggers no record requests.",
    "NAV-007: an in-app Keyboard shortcuts dialog (top-bar button plus a Help task) documents every shortcut and the guarantee that shortcuts never fire while focus is in a text field, with browser proof that j/k type into a focused field instead of navigating.",
    "Fixed a latent search bug exposed by the new coverage: clearing the query (Escape, query chip, no-results actions) now cancels the pending search debounce so a stale timer cannot silently re-apply typed text.",
    "ACC-003/010/011 (partial): authentication Show/Hide and topbar icon controls have accessible names, auth errors expose alert semantics and aria-invalid state, and the post-save confirmation is a polite atomic status region.",
    "ACC-010 (web entry-form scope): every entry-form field carries an inline role=alert error slot wired through aria-describedby; a failed save reports all problems at once (missing name, missing required secret, malformed TOTP) inline beside each field with aria-invalid state; two or more errors render a focusable error summary whose entries link to their fields, while a single error focuses its field; typed input is never discarded and editing a field clears its own error. The generic name/secret-required toasts were replaced by corrections that name the fix.",
    "ACC-011 (web scope): the automatic clipboard clear after the configured window is announced as a polite status message, including a failure variant when another app holds the clipboard (SEC-CLIP-001); the announcement does not move focus.",
    "Locale coverage for all strings across all ten catalogs at 314-key parity, and browser E2E assertions for every new behavior (comprehensive suite 37/37 plus the smoke suite).",
    "EXP-001: Settings → Data now separates `Create encrypted backup` (visible but honestly disabled/unavailable in this preview) from `Export plaintext data` as two distinct named actions.",
    "EXP-002: plaintext export opens a ceremony with an always-visible not-encrypted warning, a mandatory acknowledgement checkbox, and real master-password re-authentication against the stored verifier via a new recordSync.verifyPassword primitive that verifies without touching the session root key; wrong password or missing acknowledgement exports nothing. Browser E2E covers the refusal paths.",
    "EXP-003: the export completion view repeats `Not encrypted`, names the file (veyora-export.csv), lists the included columns (name, type, website, username, password or secret, notes) and omitted fields (TOTP seeds, favorites, custom metadata), and gives safe deletion guidance; the download filename is asserted in E2E.",
    "EXP-004 (web scope): web downloads are all-or-nothing by construction — exportLoginCsv fully serializes before the Blob/download is created and a serialization failure leaves the confirmation stage with no download; the desktop temp-file + atomic-rename fault-injection path remains open.",
    "ITEM-002: the Login form initially shows Name, Username, Password, and Website; TOTP and Notes sit behind a keyboard-accessible More fields disclosure (aria-expanded/aria-controls) that auto-expands when an edited entry already uses those fields.",
    "ITEM-003: the Login name placeholder is the GitHub - Work example and a hint under the field explains the name is for recognition.",
    "ITEM-004: secret inputs are masked by default and expose named adjacent Generate, Show/Hide (with aria-pressed state), and Copy actions; Generate was renamed from the abbreviation Gen in all ten catalogs.",
    "ITEM-005 (web implementation): the TOTP field accepts otpauth://totp/ URIs and raw Base32 via a new normalizeTotpSecret primitive (whitespace/dash stripping, uppercase normalization, stable rejection reasons), explains accepted sources in a hint, shows an inline aria-invalid error for malformed input that preserves the form and the typed value, and normalizes the value on save; unit tests cover the normalizer and browser E2E covers disclosure, field actions, malformed-input rejection, and URI-based creation.",
    "Locale coverage extended to 346-key parity across all ten catalogs (new entry.create.explainScreening and entry.create.errCommon); comprehensive browser E2E now 45/45 plus the smoke suite.",
    "UX-ONB-010 (implementation): a first-success recorder for test builds (core/telemetry.js) is opt-in only (injected VEYORA_FIRST_SUCCESS_TELEMETRY global or the veyora-first-success=1 URL parameter), measures a saved Login followed by search and copy/reveal — never Vault creation — and stores only event names and timestamps in localStorage with no Vault content; unit tests prove the ordering rule, the disabled default, and the content-free record, and a browser assertion proves a full save/search/copy/reveal session writes nothing without the opt-in. The privacy review and test-event inspection remain.",
    "UX-ONB-002 (web/shared-UI scope): the first screen now offers the four PRD routes — Create a new vault, Open an existing vault, Import from another password manager, and Advanced: connect to a self-hosted server — before any password is requested. Open states honestly when no vault is known on this device, Import explains that the master password comes first and then lands in Settings → Data, and Connect opens the Connect/Sign-in surface. Desktop-native routing evidence remains.",
    "UX-ONB-003 (web implementation): boot probes the records endpoint with the stored credential (or none); an explicit 401 renders a Connect / Sign-in gate — service URL plus the operator-issued deployment connection token, with honest copy that full device pairing arrives with connected mode — before any vault create or unlock surface. A candidate credential is verified against the service, persisted, and the app re-enters through boot so every module re-resolves the API base. Any non-401 probe outcome defers to the existing unlock error paths rather than inventing a new blocking state. Browser E2E covers the gate against a mocked 401 service (no create/unlock reachable, empty and rejected credentials refused, valid token reaching the Welcome routes); a live token-auth stack run and the sequence-8 scoped pairing model remain.",
    "Locale coverage extended to 399-key parity across all ten catalogs (18 new DIAG-001 keys: settings.diagnostics plus diag.version/build/buildNotStamped/mode/modeValue/storage/storageValue/storageUnknown/health/healthChecking/healthOk/healthUnhealthy/healthUnreachable/lastSync/syncNone/backup/backupNone); comprehensive browser E2E now 56/56 (new test: the Diagnostics panel reports version, build stamp, mode, safe storage summary, health, and last sync with no secret material anywhere in the drawer) plus the smoke suite. Earlier: Locale coverage extended to 381-key parity across all ten catalogs (7 new ITEM-006 keys: save.statusTitle/Network/Conflict/Other/Retry/Reload and modal.savedSync); comprehensive browser E2E now 55/55",
    "Fixed a first-success instrumentation flaw exposed by unit runs: achievement is judged by event-log position instead of millisecond timestamps, so sub-millisecond event bursts keep their true order (UX-ONB-010).",
    "DIAG-001 (web implementation): a Settings Diagnostics panel (apps/web/src/data/diagnostics.js plus the drawer section) shows the product version, the image build stamp (VEYORA_BUILD_COMMIT rendered into veyora-config.js at container start by deploy/web/Dockerfile and deploy/compose/compose.yaml, with the local `make run-web` path stamping from git; unstamped builds display an honest not-stamped label), the connected-vault mode, a safe storage summary (encrypted records at the origin host only — never the full URL, path, or connection token), a live service-health probe against /healthz, the last successful synchronization as a bare ISO timestamp recorded by the records client on every successful fetch or save, and an honest not-available status for the last verified backup until the sequence-5 backup flow ships. Unit tests (apps/web/test/diagnostics.test.mjs) pin the redaction boundary and the health classification; browser E2E asserts the panel contents and that no master password, entry secret, or token material appears anywhere in the settings drawer.",
    "DIAG-002 (web implementation): Settings → Diagnostics carries an opt-in `Prepare support bundle` action (apps/web/src/views/support-bundle.js). The bundle is built only on request, derived solely from the DIAG-001 safe sources through diagnostics.buildSupportBundle (version, build stamp, mode, origin host, health word, last-sync timestamp, backup status; fixed English engineering labels, like the API messages), previewed in full in an overlay that states Veyora never uploads it, and copied only through an explicit Copy button — the copy deliberately bypasses copyWithTimeout so it cannot advance the Start-here checklist or emit a first-success secret event, and no automatic submission path exists. Unit canary tests prove a seeded connection token, full URLs, and paths cannot reach the bundle text; browser E2E asserts the opt-in gating, preview contents, redaction, and the no-automatic-upload statement (7 new locale keys, 407-key parity).",
    "DIAG-003 (complete): the API error catalog is closed and completeness is machine-enforced by blocking cargo tests in services/api/src/error_catalog.rs — every `PM-*` literal used in the API sources must be cataloged (every_source_code_literal_is_cataloged), every catalog code must be searchable in docs/reference/api.md and the documentation may not reference uncataloged codes (documentation_and_catalog_are_in_sync), and codes must be unique, uppercase ASCII, and well-formed. Real gaps were fixed: axum Json/Query extractor rejections previously escaped the envelope as plain text and now return the stable-code JSON envelope (PM-API-BAD-BODY for bodies, the new PM-API-BAD-QUERY for query strings), and the documentation table gained the previously missing PM-API-RATE-LIMITED plus PM-API-BAD-QUERY with the closed-catalog guarantee stated. The web client ships apiError.PM-API-BAD-QUERY in all ten catalogs. Verified by cargo test -p api (33 tests), live-stack envelope checks, and the API smoke suite.",
    "EXP-004 (complete): the desktop native file-writing path now finalizes atomically. A new helper (apps/desktop/src-tauri/src/atomic_file.rs) writes every user-save-dialog export (Vault -> Export Backup) to a hidden temporary file beside the target, flushes it to disk, and only then atomically renames it over the target; any failure deletes the temporary file and leaves a previously valid target untouched, and startup snapshot copies in vault.rs gained the same partial-file cleanup. Five fault-injection unit tests (cargo test -p veyora-desktop) prove the old file survives injected create/write/flush failures byte-identical, partial temporaries are always removed, and successful writes leave no residue. The web path remains all-or-nothing by construction.",
    "UX-ONB-003 live token-auth verification: a new on-demand browser script (tests/e2e/web/verify-token-gate.mjs, requires VEYORA_CONNECT_TOKEN) ran against a real compose stack started with VEYORA_API_AUTH=token + VEYORA_API_TOKEN and passed 4/4 - the live API returns the stable PM-API-UNAUTHORIZED envelope for missing and wrong tokens and 200 for the real one, a fresh browser renders the Connect / Sign-in gate with no vault-create/unlock surface, the live service refuses a wrong connection token keeping the gate up, and the real deployment token enters the Welcome routes. The sequence-8 scoped pairing model remains.",
    "ACC-002/003/006/007/008 (automated web scope): a new blocking browser suite (tests/e2e/web/test-browser-accessibility.mjs, also wired into CI after a pristine-store reset) audits the Welcome routes, Connect gate, create form, item dashboard, entry modal, password generator, item drawer, Settings drawer, and Help overlay for accessible names on every visible control, persistent visible labels on data-entry fields (placeholder-only identification is rejected; view controls such as search and sort order require an accessible name), rendered text contrast against both light and dark themes (4.5:1 normal / 3:1 large text, with element opacity blended in), 24x24 CSS px pointer targets, and 320px reflow (no document-level horizontal scrolling, primary actions on-screen); 18/18 pass locally. Defects the audit exposed were fixed in apps/web: 1:1 contrast on the focused/hovered Welcome route tile and the selected item-type template (text now inverts on the filled surface), the tertiary ink-3 token now meets 4.5:1 in both themes (light #6f6f6f, dark #8a8a8a), template type codes dropped a failing 0.55 opacity for the compliant token color, the More fields disclosure no longer turns white-on-white on hover and meets the 24px target, the custom checkbox and password-length slider were resized to 24px hit areas, the settings selects (aria-labelledby to their visible row labels), the sort-order select, and the generator option checkboxes gained accessible names, and the top bar wraps at <=640px (full-width search row, wrapping actions, shortcuts entry hidden as a non-core documented control) removing 63px of forced horizontal page scrolling at 320px. The manual keyboard/screen-reader/zoom/native portions of E2E-010 and ACC-001/004/005/012/013 remain.",
    "ACC-004/005 automated scope plus 200% zoom reflow: a second blocking browser suite (tests/e2e/web/test-browser-keyboard.mjs, wired into CI after a pristine-store reset like the other suites) proves the WAI-ARIA dialog pattern for the entry modal, password generator, settings drawer, Help overlay, and shortcuts dialog — focus moves into the dialog when it opens, Tab/Shift+Tab cycles never leave it, and closing with Escape or the close/cancel control returns focus to the opener (including the generator layered over the entry form, where Escape closes both layers and focus lands back on New item); verifies ACC-005 by focusing every control reachable in the current context (the topmost open dialog when one is open, since the dialog pattern makes the page behind inert) on the dashboard, entry modal, and settings drawer and asserting the element at the interaction point is the control or its own content; and checks 200% zoom reflow at 640 CSS px (half of the 1280 base viewport) with no document-level horizontal scrolling and primary actions on-screen for the dashboard, entry modal, and settings drawer. 20/20 pass across three consecutive local runs. The audit exposed and fixed three real defects: the main.js focus trap selected the first open overlay in DOM order instead of the topmost (the generator opened over the entry form could escape into the modal behind, and an overlay over the drawer trapped the wrong container; the trap now takes the last open overlay and prefers it over the drawer), the dashboard theme-toggle button rendered empty because applyDocumentTheme ran before the dashboard existed (the icon is now in the initial markup), and the fixed 34px .tb-lock width made the text-bearing shortcuts/help buttons paint their content over adjacent topbar buttons (now auto-sized with a 34px floor and gap, with the wrapped actions row width-bounded at <=640px so the 320px reflow audit still passes 18/18). A generic focus-return helper (captureFocusReturn/restoreFocusReturn in apps/web/src/core/ui.js) is wired into every dialog surface: entry modal, generator, settings/entry drawer, export ceremony (including the completion view's close), support-bundle preview, and shortcuts dialog; the generator now also moves focus to its length control on open so the trap holds from the first press. Manual screen-reader (VoiceOver/NVDA), native-desktop, and remaining zoom/manual evidence for ACC-001/004/012/013 and E2E-010 still remain.",
    "Manual-evidence capture kit (preparation only, not evidence): docs/evidence/manual-evidence/ now holds structured Pending record templates for all 21 pending manual activities (ACC-001, ACC-004/012/013, the E2E-010 manual portion, UX-ONB-001/004 copy review, UX-ONB-002 desktop routing, UX-ONB-005 autofill/platform matrix, UX-ONB-006 threat-model approval, UX-ONB-010 privacy review, EXP-002/003 and HELP-002 copy reviews, ITEM-002..005 form studies, and the DIAG-001/DIAG-002/ITEM-006 desktop portions). tools/lint/check-manual-evidence.py (run by make check and the CI repository job) enforces record structure: every expected requirement ID covered exactly once, Pending records leave the completion fields empty, and any In-progress/Pass/Fail record must carry a method, environment, named operator, an ISO date not in the future, notes (Pass/Fail), and at least one artifact path that exists in the repository or an https location; Fail records must state a follow-up. The templates prepare but never impersonate the required human evidence — a record counts only after a human performs the activity and fills it in.",
    "DIAG-001/DIAG-002 desktop-runtime alignment (web-code scope consumed by the desktop shell): diagnostics.isDesktopRuntime() branches on the Tauri-injected window.VEYORA_DESKTOP flag, so the Settings Diagnostics panel shows `Local vault on this device` mode and `Encrypted vault stored on this device` storage (never `Connected vault`, never the meaningless 127.0.0.1 loopback host), and buildSupportBundle() prints `mode: desktop-local-vault` plus `storage: local vault on this device` with no `service-host:` line on desktop; connected-mode output is byte-identical to before. Two new locale keys (diag.modeValueDesktop, diag.storageValueDesktop) ship in all ten catalogs (409-key parity); unit tests pin the desktop branch and its no-loopback/no-service-host canaries, and a new comprehensive-E2E assertion sets the flag and verifies the drawer plus bundle rendering (suite now 58/58).",
    "UX-ONB-002 desktop-native first-run implementation (Tauri shell): the setup screen now offers all four Welcome routes instead of one ambiguous storage-location button. `Create a new vault` refuses a folder that already contains vault.db with a pointer to the Open route; `Open an existing vault` requires an existing database (honestly stating none is known on this device yet); `Import from another password manager` creates a vault and states that the next screen offers the import; `Advanced: connect to a self-hosted server` explains that this preview keeps the vault local and opens a shell-validated plain http/https address in the system browser (open_external_url command; whitespace/control/non-http schemes rejected). Implementation: pick_vault_dir gained a `route` parameter with create/open guards (SetupRoute in apps/desktop/src-tauri/src/lib.rs) so neither route can silently become the other, and setup.js renders the four routes with honest copy before any password is requested. Verified by cargo fmt --check, clippy -D warnings, and cargo test -p veyora-desktop (now 9/9: four new tests pin the route guards, route id parsing, and external-URL validation); apps/desktop/README.md and docs/DESKTOP.md describe the four routes. Native desktop execution evidence for the routes remains.",
    "Desktop first-run dialog/status accessibility alignment (ACC-004 pattern, desktop shell): the setup overlay is now exposed to assistive technology as a named modal dialog (role=dialog, aria-modal, aria-labelledby to the Welcome heading), its waiting/error line is a live role=status region matching the PRD 7.3 error model, focus lands on the first route action when the wizard opens, Enter in the connect address field triggers Open in Browser, and every control shows a visible focus outline. apps/desktop/src-tauri/src/setup.js only; verified with node --check, cargo fmt --check, clippy -D warnings, and cargo test -p veyora-desktop 9/9 (Rust unchanged); docs/DESKTOP.md documents the dialog behavior. Native desktop screen-reader execution evidence remains a human activity.",
    "Vault identity on locked and unlocked screens (PRD 6.3 / NAV-001, web scope consumed by the desktop shell): the dashboard top bar carries a human-readable vault name plus a safe location summary (diagnostics.vaultIdentity(): 'On this device' on the desktop runtime, the connected service's origin host alone on the web), the locked unlock screen's identity line uses the same name and location instead of the raw vault-id hex, and unit tests plus browser assertions (both runtimes, with URL/token canaries) pin the boundary. 4 new locale keys ship in all ten catalogs (413-key parity); comprehensive browser E2E now 59/59 plus the smoke suite; web unit tests 77/77; accessibility 18/18 and keyboard 20/20 unchanged.",
    "ITEM-006 offline pending queue (connected web scope): when the service is unreachable, saveEntry queues the already-sealed ciphertext DTO in a per-vault localStorage queue (apps/web/src/core/pending-queue.js; a newer queued write replaces the earlier one for the same record) and rethrows with queued=true, the entry modal commits the item to session state and shows a saved panel reporting 'Saved on this device — not yet on the service' with a Sync now action, the dashboard renders a Pending sync badge on rows whose id is still queued, and recordSync.flushPending() — run before every listing in fetchAll and from the panel's Sync now — replays queued writes (keeping the queue while still offline, committing successful replays, and dropping entries the server definitively rejects, e.g. compare-and-set conflicts). Unit tests cover the queue store and the save/flush/conflict/replay-order behaviors with a no-plaintext-in-storage canary; browser E2E aborts the record PUT, observes the pending report, badge, and ciphertext-only storage, reconnects, and proves the synchronized report plus badge clearing. 3 new locale keys in all ten catalogs (416-key parity); web unit tests 85/85; comprehensive browser E2E 60/60; accessibility 18/18, keyboard 20/20, browser + API smoke unchanged.",
    "Desktop-runtime honesty for pending/save copy plus an API-003 CORS fix (slice twenty-one): the API CORS preflight now returns 'access-control-allow-headers: authorization, content-type' so a cross-origin token deployment's preflight is no longer rejected (services/api/src/lib.rs; verified by a live OPTIONS request against the rebuilt compose stack, cargo test -p api 31+ tests, fmt, clippy -D warnings), and the desktop runtime no longer describes a remote service it does not have — when window.VEYORA_DESKTOP is set the pending-save panel says the local vault did not respond with a 'Try saving again' action instead of 'not yet on the service' + 'Sync now', the row badge title is 'Pending save', and the Diagnostics timestamp row reads 'Last saved'/'Nothing saved yet' instead of 'Last sync'/'Not synchronized yet' (modal/dashboard/drawer views; 5 new locale keys, 421-key parity in all ten catalogs). The comprehensive browser suite gained desktop-mode assertions for the Last saved row (still 60/60); web unit tests, accessibility 18/18, keyboard 20/20, browser + API smoke all pass. The desktop pending-copy branch itself is exercised only by the connected-mode offline test plus locale parity (no native desktop offline E2E exists); native desktop evidence remains the human activity.",
    "PRD 7.1 tabs: Favorites and Tags are real scoped views. Tags are assigned per entry behind the More-fields disclosure (ITEM-002 wording), parsed from a comma-separated list, stored on the encrypted item, matched by search (NAV-006), and surfaced as a distinct-tags tab count with removable pressed-state filter chips (NAV-003). Blocking browser journeys cover favorite/unfavorite scoping and tag create/assign/filter/search end-to-end (tests/e2e/web/test-browser-full.mjs, 62/62 green twice consecutively from pristine stores; smoke, accessibility 18/18, and keyboard 20/20 suites green).",
    "Browser coverage for every non-Login template: blocking journeys create Secure Note, API Token, SSH Key, and Identity entries through the template picker, assert each row's localized type label, prove type-tab scoping lists only that template's entries, and prove type-specific fields (service subtitle, host) are searchable with zero service requests (NAV-006). tests/e2e/web/test-browser-full.mjs now runs 64 tests, green twice consecutively from pristine stores.",
    "ITEM-006 browser fault coverage with real service answers: a new fault suite (tests/e2e/web/test-browser-faults.mjs, make test-browser-faults) drives a genuine 429 PM-API-RATE-LIMITED (Retry-After honored) from the live rate limiter and a genuine 503 PM-STORE-UNAVAILABLE from a stopped store into the save banner, asserts the typed input is preserved and the failure class is named, and recovers both legs for real (minute-window reset; store restart with readiness flipping back to ready). Green twice consecutively on its disposable rate-limited stack.",
    "Accessibility browser coverage widened: the accessibility suite emulates prefers-reduced-motion and proves every animation/transition duration collapses to effectively zero (19/19 green twice consecutively), and the comprehensive suite's Arabic leg now proves RTL is a rendering state — mirrored dashboard chrome, zero horizontal overflow, and Arabic tab copy (64/64 green twice consecutively). The honest browser inventory gap list shrinks to slow/double-submit requests and native-desktop execution.",
    "Slow-response and double-submit coverage with real latency: the fault stack's gateway now injects genuine per-route latency on record writes (VEYORA_GATEWAY_DELAY_MS; the Envoy fault block is deleted entirely unless the knob is set, and health/readiness probes stay instant). The fault suite proves the save control goes inert while the write is in flight, that hammering it fires exactly one record write, and that the slow save still commits exactly one entry (3/3 green twice consecutively; the standard gateway stays delay-free with the comprehensive suite 64/64). The browser inventory gap list now holds only native-desktop execution.",
    "EXP-001 web scope complete: 'Create encrypted backup' is now a real available action (previously honestly-unavailable) with a full ceremony, plus 'Restore backup' with merge semantics; the settings copy no longer claims unavailability (473-key parity across all ten catalogs) and the blocking EXP-001 browser assertions now pin availability, portability copy, and the separate plaintext-export warning.",
    "UX-ONB-007 is complete (web scope): vault creation generates the real Recovery Key, presents it immediately after the vault opens (canonical 71-character form, copy action, storage guidance warning that Veyora cannot recover it), and requires a verified save — the user re-enters the key and the kernel codec plus exact-match check must pass before the onboarding step completes; a mistyped copy is refused inline. The start-here checklist's recovery and backup steps are now real (no more honest-unavailable notes): recovery completes through the creation ceremony and backup through a successful encrypted-backup download, and the card hides only when all five steps are done. Settings → Security also offers View/verify to repeat the ceremony anytime. Blocking browser journeys cover the full ceremony (wrong entry refused, verified save recorded) and every suite's creation helper now walks the real ceremony — comprehensive 69/69 twice consecutively plus smoke, a11y 19/19, keyboard 20/20, journeys 6/6, fault 4/4; 500-key parity.",
    "ITEM-008 is complete in the web scope: the Trash tab now offers permanent deletion with a concrete, two-step armed confirmation that names the item count and irreversibility (and an explicit note that encrypted copies leave the service immediately, ahead of the retention window — ITEM-009's 'unless the user purges earlier'). The purge runs the same scoped store operation the worker's retention policy uses. A blocking browser journey proves the count-naming button, the cannot-be-undone armed state, the emptied scope, and a server-side audit that the vault holds zero tombstones afterwards while every live item is untouched (comprehensive 70/70 twice consecutively; smoke, a11y 19/19, keyboard 20/20, journeys 6/6, fault 4/4; 108/108 web units; 504-key parity × 10 catalogs).",
    "Browser locale sweep (goal acceptance: all supported languages re-render after switch): a new blocking journey cycles all nine non-English catalogs (zh-CN, zh-TW, ja, ko, de, fr, es, ru, ar) through the live language switcher, asserting per locale the registry direction (ar rtl, the rest ltr), a rendered translated tab label, unchanged row count (no state loss), and zero horizontal overflow — then restores English. Combined with the existing 504-key parity check and the deep Arabic RTL assertions, every supported locale is now exercised in a real browser. Comprehensive suite 71/71 twice consecutively from pristine stores.",
    "UX-ONB-003's live token-auth verification now runs in CI on every push: the Compose-integration job restarts the stack in VEYORA_API_AUTH=token mode after the browser suites and executes tests/e2e/web/verify-token-gate.mjs against it — the previously on-demand-only script (stable 401 envelope for missing/wrong tokens, fresh-browser Connect gate, wrong-token refusal, real deployment token entering the Welcome routes). Wiring it surfaced and fixed a staleness defect: the authorized probe listed /records unscoped, which since DATA-004 answers 400 on the missing declared scope — the probe now carries ?vault=probe and the full 4/4 verification passes against a real token stack.",
    "UX-ONB-010 test-event inspection (the harness evidence the PRD asks for): a new blocking journey opts in via the documented ?veyora-first-success=1 parameter, drives the success path (vault create + kit verify + login save + search), and inspects the localStorage record exactly as the harness is documented to — asserting events carry only the event slug and timestamp (exact key set), login-saved and search-performed are present, and the record never contains the item name, secret material, or the search query. Building it exposed and fixed a test-side race (the row locator was satisfied before the search debounce fired, so Escape cancelled the pending search) and aligned the journey to the real event schema ({event, at}). Comprehensive suite 72/72 twice consecutively from pristine stores; the opt-out journey remains alongside it."
   ],
   "remaining_scope": [
    "Complete Welcome, core UX, error, settings, help, and accessibility requirements, including desktop-native Welcome routing evidence for UX-ONB-002, the sequence-8 scoped pairing replacement of the UX-ONB-003 gate, the manual/autofill portion of UX-ONB-005, threat-model approval for UX-ONB-006, the privacy review and test-event inspection for UX-ONB-010, ITEM-006 desktop/native evidence, the ITEM-002..005 form-task and keyboard/screen-reader manual studies EXP UX review of the warning/completion copy, the desktop/native Diagnostics and support-bundle evidence behind DIAG-001/DIAG-002, the manual keyboard/screen-reader/200%-zoom/desktop evidence behind ACC-001/004/005/012/013 (contrast, reflow, target size, labels, and accessible names are now machine-checked by tests/e2e/web/test-browser-accessibility.mjs), and the manual screen-reader portion of ACC-010/011.",
    "Obtain UX-ONB-001/004 copy review and the 30-second comprehension study, HELP-002 content inventory review, and desktop/native Help and accessibility evidence.",
    "Complete ITEM-006 desktop/native coverage and any remaining status-reporting polish (web implementation exists: not-saved banner with idempotent retry, conflict report with reload, synchronized-revision success reporting, and a true offline pending queue with local persistence of sealed ciphertext DTOs, pending badges, Sync now, and pre-listing replay), ITEM-008 permanent-deletion confirmation that names item count and irreversibility, and ITEM-009/DATA-008 scoped retention enforcement.",
    "Pass E2E-001 and E2E-010 and complete the required user study."
   ],
   "evidence": [
    "apps/web/src/views/dashboard.js",
    "apps/web/src/views/entry-flow.js",
    "apps/web/src/views/drawer.js",
    "apps/web/src/views/modals.js",
    "apps/web/src/views/help.js",
    "apps/web/src/views/start-here.js",
    "apps/web/src/views/shortcuts.js",
    "apps/web/src/core/checklist.js",
    "apps/web/src/data/search.js",
    "apps/web/src/config.js",
    "apps/web/src/core/state.js",
    "apps/web/src/core/ui.js",
    "apps/web/src/styles/components.css",
    "apps/web/locales/en.json",
    "apps/web/test/units.test.mjs",
    "apps/web/tools/check-locales.mjs",
    "docs/USER-GUIDE.md",
    "tests/e2e/web/test-browser-full.mjs",
    "apps/web/src/views/export.js",
    "apps/web/src/data/totp.js",
    "apps/web/src/data/schema.js",
    "apps/web/test/totp.test.mjs",
    "apps/web/src/core/records.js",
    "apps/web/src/data/blocklist.js",
    "apps/web/src/core/telemetry.js",
    "apps/web/src/views/connect.js",
    "apps/web/test/blocklist.test.mjs",
    "apps/web/test/telemetry.test.mjs",
    "apps/web/src/views/modals.js",
    "apps/web/src/core/ui.js",
    "apps/web/src/styles/components.css",
    "apps/web/test/connect.test.mjs",
    "docs/USER-GUIDE.md",
    "apps/web/src/data/diagnostics.js",
    "apps/web/test/diagnostics.test.mjs",
    "services/api/src/error_catalog.rs",
    "docs/reference/api.md",
    "apps/web/src/views/support-bundle.js",
    "apps/desktop/src-tauri/src/atomic_file.rs",
    "tests/e2e/web/verify-token-gate.mjs",
    "tests/e2e/web/test-browser-accessibility.mjs",
    "tests/e2e/web/test-browser-keyboard.mjs",
    "apps/web/src/styles/tokens.css",
    "apps/web/src/styles/components.css",
    "apps/web/src/core/ui.js",
    "apps/web/src/main.js",
    "apps/web/src/views/dashboard.js",
    "docs/evidence/manual-evidence/README.md",
    "docs/evidence/manual-evidence/accessibility-manual.md",
    "docs/evidence/manual-evidence/ux-copy-review.md",
    "docs/evidence/manual-evidence/form-task-studies.md",
    "docs/evidence/manual-evidence/onboarding-manual.md",
    "docs/evidence/manual-evidence/desktop-native.md",
    "tools/lint/check-manual-evidence.py",
    "apps/web/src/data/diagnostics.js",
    "apps/web/test/diagnostics.test.mjs",
    "apps/desktop/src-tauri/src/setup.js",
    "apps/desktop/src-tauri/src/lib.rs",
    "apps/desktop/README.md",
    "docs/DESKTOP.md",
    "apps/web/src/core/pending-queue.js",
    "apps/web/src/core/records.js",
    "apps/web/src/views/modals.js",
    "apps/web/test/pending-queue.test.mjs",
    "apps/web/test/records.test.mjs",
    "docs/browser-test-inventory.md",
    "apps/web/locales/fr.json",
    "apps/web/locales/de.json",
    "apps/web/locales/es.json",
    "apps/web/locales/ru.json",
    "apps/web/locales/ja.json",
    "apps/web/locales/ar.json",
    "apps/web/locales/ko.json",
    "apps/web/locales/zh-CN.json",
    "apps/web/locales/zh-TW.json",
    "tests/e2e/web/test-browser-faults.mjs",
    "deploy/gateway/envoy-plain.yaml",
    "deploy/gateway/entrypoint.sh"
   ]
  },
  {
   "sequence": 8,
   "work_package": "Implement connected auth and production topology",
   "primary_ids": [
    "API-*",
    "DEP-*",
    "DB-*"
   ],
   "status": "Partial",
   "completed_scope": [
    "API-006: /readyz now answers 503 with an honest ready=false body when the backing store is unreachable so orchestrators stop routing traffic, while /healthz stays process-alive-only (200 during a database outage) so an alive process is not restarted; covered by a new unavailable-store test (cargo test -p api 33 tests, fmt, clippy -D warnings).",
    "DEP-006 (mechanical scope): all six Rust service images (api, worker, migrator, backup, restore, validator) run as an unprivileged fixed uid 10001 veyora user, and compose drops ALL capabilities plus sets read_only root filesystems with an ephemeral /tmp for api, worker, migrator, and backup; gateway keeps cap_drop ALL, web keeps cap_drop ALL with the minimal documented nginx necessity set (CHOWN, NET_BIND_SERVICE, SETGID, SETUID), and postgres is a documented exception (initdb must chown its data volume). Verified live: rebuilt compose stack fully healthy (api healthy, worker up, gateway up, web up), every Rust container runs as uid 10001, and tests/smoke/api.sh passes all checks; docker compose config validates.",
    "DEP-006 (completion for gateway and web): the gateway entrypoint now renders its envoy configuration into the ephemeral /tmp mount instead of /etc/envoy and the image runs as the unprivileged envoy user (uid 101, verified live), so it takes the full hardening profile (cap_drop ALL + read_only + tmpfs); the web image stages its nginx server config as a read-only /etc/nginx/template and renders it at start into the tmpfs-mounted /etc/nginx/conf.d, serves the runtime-rendered veyora-config.js from /tmp through a new exact-match location, and runs with a read-only root filesystem (tmpfs on /tmp, /etc/nginx/conf.d, /var/cache/nginx, /var/run) still holding the documented minimal nginx capability set. Live-verified on the rebuilt hardened stack: all services healthy, web /, /veyora-config.js (correct content), /api proxy, .wasm/.js assets, gateway /healthz all 200, gateway confirmed at uid 101, tests/smoke/api.sh ALL CHECKS PASSED, and the browser smoke suite (derive, seal, store, decrypt, reveal, delete, lock) passed against the read-only stack.",
    "Fixed a latent backup-profile bug exposed by the hardening pass: the compose backup entrypoint invoked a nonexistent ./backup binary; it now runs veyora-backup, and the ./backups host bind mount documents the one-time install -d -o 10001 ownership step the non-root container requires.",
    "DEP-007 (partial): base-image digests are pinned upstream in the release path (the publish workflow and tools/release/publish-containers.sh mirror digest-pinned foundations into GHCR and application builds consume only those mirrors), the gateway gained a process-liveness healthcheck (TCP probe of its data-path listener via bash /dev/tcp — the envoy image ships no HTTP probe tool), the web container gained a listener healthcheck (busybox wget), compose declares bounded per-service memory limits (postgres 1g, api 512m, worker/migrator/gateway/backup 256m, web 128m — verified applied via docker inspect), and the api service documents a 15s stop_graceperiod window matching its in-flight request drain on SIGTERM. Live-verified: all five long-running services report healthy on the fully hardened stack and both smoke suites (API + browser) pass.",
    "DEP-013 (complete): destructive teardown now has an explicit, named confirmation path — a new make purge-data target refuses to run unless CONFIRM=destroy-veyora-data is passed exactly (verified for the refusal, wrong-value, and armed paths), and the deployment/operator docs state precisely what docker compose down --volumes deletes (the named veyora-pg volume holding every encrypted record) and what it does not touch (./backups bind mount, anything outside Compose, no undelete).",
    "DB-002 (complete): the PostgreSQL connection pool has an explicit, bounded, environment-tunable policy — maximum size (8), acquisition timeout (3 s), session-level statement timeout (15 s), and idle timeout (300 s) via VEYORA_DB_POOL_MAX / VEYORA_DB_ACQUIRE_TIMEOUT_MS / VEYORA_DB_STATEMENT_TIMEOUT_MS / VEYORA_DB_IDLE_TIMEOUT_SECS. Acquisition blocks on a condition variable up to the timeout and then fails closed as PM-STORE-UNAVAILABLE instead of growing unbounded; every connection (initial, per-open, and reused) carries SET statement_timeout so a wedged query cannot pin its connection; idle-expired connections are discarded rather than reused; and PostgresStore::shutdown() closes idle connections immediately and waits (bounded) for checked-out connections to return, after which checkouts fail closed. Verified: cargo fmt/clippy -D warnings across the root workspace, new unit tests (policy bounds, override flooring, hard-cap reservation), and the live --ignored integration test against a real compose PostgreSQL now also proving the statement_timeout session setting and shutdown fail-closed behavior.",
    "DEC-002 design work (proposed, review-gated like sequence 4): ADR 0005 defines the scoped pairing/session credential lifecycle — a 32-byte CSPRNG connection credential bound to principal/device/generation/purpose, hashed server-side, single-use short-TTL pairing codes provisioned by the operator deployment token (which never authorizes records or reaches clients), bounded-overlap rotation, immediate idempotent revocation, absolute+idle expiry, desktop OS-credential-store and web HttpOnly/Secure/SameSite=Strict cookie storage with localStorage bearer tokens forbidden, server-side scope enforcement, and a closed PM-AUTH-* error surface. The machine contract (contracts/protocol/pairing-session-v1.json + schema), checker (tools/lint/check-pairing-session.py, wired into make check and the CI repository job alongside the contract regression tests), and 11 negative tests (tests/contracts/test_pairing_session.py) enforce the invariants and keep the status Proposed until a qualified independent review is recorded. make check passes (repository + manual-evidence + pairing + 18 contract tests).",
    "DEP-015 (complete): docs/runbook.md covers every PRD-required failure mode — TLS, authentication (including token rotation and rate-limit behavior), CORS (with the exact preflight verification command), database (bounded-pool exhaustion semantics), migration, backup (uid-10001 ownership), restore (explicit policy and real counts), storage exhaustion, clock drift, and client/server version mismatch — each with concrete docker/curl diagnostics anchored on /healthz vs /readyz semantics, plus a destructive-operations summary pointing at the CONFIRM-gated purge.",
    "DEP-012 (complete for the documented procedure): the OPERATOR-GUIDE upgrade section is now a bounded preflight -> verified backup -> switch -> verify -> rollback-decision sequence with recorded version checks, a live-verified one-shot backup command (docker compose run --rm --entrypoint veyora-backup backup — verified against a real compose PostgreSQL after the migrator gate, producing a valid snapshot), migrator-gated switchover, health/readiness/smoke verification, and data-format-limited rollback guidance.",
    "DEP-009/SEC-WEB-003 (web-origin scope): the drifted security-header contract was corrected to the actually reviewed served values (CSP with the documented narrow wasm-unsafe-eval and style-src allowances, no-referrer Referrer-Policy) and the web origin now serves the previously missing deny-by-default Permissions-Policy plus Cross-Origin-Opener/Resource-Policy same-origin headers; a new contract-enforcement smoke test (tests/smoke/headers.sh, shellcheck-clean and wired into the CI lint list) compares every live header against contracts/web/security-headers-v1.json and rejects forbidden script-src 'unsafe-eval' plus missing object-src/base-uri/frame-ancestors 'none'. Live-verified against the rebuilt web image (7/7 headers OK), with the browser and API smoke suites still passing. The gateway/TLS-origin header story and real-HTTPS external scan remain with E2E-008.",
    "DEP-007 remainder (digest drift): tools/lint/check-foundation-pins.py (wired into make check and CI) proves the publish workflow and tools/release/publish-containers.sh mirror exactly the same five digest-pinned upstream foundations and that every Dockerfile default base tag is the tag side of one of those pins; a drifted digest is caught (negative-verified by injecting a fake digest).",
    "DEP-007 (architecture inspection): tools/release/inspect-image.py reports every linux platform entry of an image's OCI index (docker buildx imagetools --raw) with an optional --require gate for the release matrix (amd64+arm64). Verified against a public multi-arch image (positive and a genuinely missing-architecture negative that exits 1) and against the published ghcr.io veyora-api image, which the tool proves is a true linux/amd64+linux/arm64 index. Pulling and starting every image on both real architectures remains the runner-side release gate.",
    "DEP-003 (complete for the Compose topology): tools/lint/check-compose-topology.py (wired into make check and CI) renders the canonical Compose file and enforces the exposure contract — only the gateway's TLS edge port (8443) may bind non-loopback, every other published host port must be loopback-only, and the internal services (api, worker, migrator, backup, restore, validator) must publish no host ports at all. Verified against the real rendered topology (PASS) and negative cases (an internal service publishing a port and a non-loopback non-edge binding are both rejected). An external port scan against a deployed host remains part of E2E-008.",
    "DB-003 (complete): remote-database TLS is configurable and defaults safely through a pure-Rust stack (rustls with the ring provider — no OpenSSL system packages, aws-lc explicitly excluded via default-features-off on rustls and tokio-rustls so container images need no new system dependencies). VEYORA_DB_TLS_MODE selects verify-full / disabled-insecure / auto (default): every non-loopback TCP host requires verified TLS unless the operator explicitly acknowledges the private network (Compose now carries that acknowledgment for its container network), unknown modes fail closed, and VEYORA_DB_TLS_CA_FILE adds the operator's private CA (a configured-but-missing CA file fails closed rather than skipping private roots). Verified: new unit tests (remote default, local plaintext, override flooring, missing-CA fail-closed), and a live integration test against a real ssl=on PostgreSQL with a self-signed private CA — verify-full connects and migrates, verify-full without the CA fails, and the rebuilt Compose stack (Rust images rebuilt on the bookworm builder after pinning rust:1.93.1-slim-bookworm@sha256:… to keep builder and runtime glibc aligned — the trixie builder had produced GLIBC_2.38 symbols in aws-lc/ring C code) passes the API smoke, header smoke, and browser smoke suites.",
    "DEP-002 (fix + TLS rehearsal): the gateway's HTTP-to-HTTPS redirect previously constructed https://<host>:8080 (the plain port) via a Location header string; it now uses Envoy's redirect action with scheme_redirect plus port_redirect rendered from the TLS port, so no invalid destination can be constructed. Verified live with self-signed certificates in the Compose TLS mode: 301 Location now targets https://127.0.0.1:8443, TLS /healthz and /readyz answer 200, and a full PUT(201)/DELETE(200) round trip runs over verified TLS. A real public hostname with a CA-issued certificate remains part of E2E-008.",
    "DB-004 (complete): least-privilege database roles are provisioned and enforced. deploy/containers/postgres/init-roles.sh runs once on first initialization (psql-quoted passwords; role-specific VEYORA_DB_APP_PASSWORD / VEYORA_DB_MIGRATOR_PASSWORD fall back to the shared VEYORA_DB_PASSWORD) creating veyora_migrator (CREATE/USAGE on public — DDL) and veyora_app (USAGE only — DML); the records migration, applied by the migrator role as table owner, grants veyora_app exactly SELECT/INSERT/UPDATE/DELETE (role-conditional so role-less local databases still migrate); Compose gives the migrator and every running service their own role-scoped DATABASE_URL; and the API startup now falls back from migrate() to a new verify_schema() path so the DML-only role starts after the migrator gate without holding DDL. Live-verified on a fresh role-initialized stack: all services healthy, API/header smokes pass, and the new ignored live test proves the DML role is denied DDL while data operations and schema verification work, with the DDL role able to migrate.",
    "DEP-014 (complete for every operational service): the UTC ISO-8601 formatter lives once in the std-only shared persistence crate (unit-tested against the epoch, a leap day, and known instants) and every operational service prefixes its log lines with it — API access logs (with restart-unique, bounded-cardinality boot-prefixed request IDs echoed in X-Request-Id), the worker's poll lines, the migrator's applied-migration lines, and backup/restore completion lines. Log content is counts and stable English events only (no record IDs, Vault IDs, paths, or ciphertext; the API metrics surface carries the same guarantee by the API-007 canary test). Live-verified on the rebuilt stack: '2026-09-05T05:26:27Z backup: exported 1 record(s)' and worker/migrator UTC lines observed, API smoke green. A formal observability review remains part of E2E-008's operations evidence.",
    "API-007 (complete for the current metrics surface): a blocking cargo canary test writes a record with a known record ID and vault ID, then asserts the /metrics output contains neither (nor any record path) — the metrics endpoint stays bounded-cardinality counters only by construction and by test (cargo test -p api 35 tests).",
    "PostgreSQL pool recovery (DB-002 hardening): idle connections are now probed with a SELECT 1 round trip before hand-out, so a database restart no longer strands dead sockets in the pool (previously every store call failed forever after a blip and /readyz never returned to ready). Proven live by the browser fault suite: postgres is stopped mid-run, the API answers 503 PM-STORE-UNAVAILABLE, then after restart /readyz returns to 200 and the retried save commits (backend-postgres 6/6 unit tests, api 41/41).",
    "Backup/wipe/restore drill made real, machine-run, and CI-wired: executing the existing tests/integration/backup-restore.sh end-to-end for the first time exposed two genuine defects — (1) the restore binary's INSERT used ON CONFLICT (record_id), the pre-DATA-001 single-column conflict target, so restore has been failing against the composite-(vault_id, record_id) schema ('insert ... db error'); now ON CONFLICT (vault_id, record_id). (2) the drill itself never wiped, never fail-failed the create, used a fixed record id (409 on re-runs), and read without the declared vault scope — rewritten: unique per-run record, scoped create/verify (DATA-004), curl --fail, a real TRUNCATE wipe (psql or the postgres container), and correct binary defaults. Verified green via `make test-backup-restore` (create → backup → TRUNCATE → restore → byte-level ciphertext verification over the live stack's PostgreSQL) and wired into the CI Compose-integration job so runners execute it against the stack database on every push.",
    "DEP-007 runner-side remainder wired: the publish-images workflow's verify job now runs the dedicated tools/release/inspect-image.py with --require amd64,arm64 over every one of the 13 published images (anonymous access proving public readability plus the tool's architecture gate, replacing the inline grep), and a new pull-and-execute step pulls veyora-backup at the release tag and proves the published bytes actually run on the runner — the binary's honest 'DATABASE_URL is required' refusal (exit 1) is the executability evidence. The workflow is dispatch-gated on the exact version-authority tag and immutable release refs, so this executes on the owner's next authorized release run; the tooling is in place and mechanically linted (actions SHA-pinned, make check green).",
    "QA-011 quickstart execution in CI + a machine-proven stale-image finding: a dedicated job now executes the README's local-preview commands verbatim (clone → cp .env.example → password → compose up → documented URL checks → tests/smoke/api.sh → documented teardown), and the Repository integrity job runs the release auditor's dry-run so the verification command's repository contract is continuously checked. First execution surfaced a genuine gap: the published v1.0.0 application images predate the DB-004 role-scoped startup topology — the stale API image exits at boot when it lands on the DML-only role, so the verbatim quickstart fails against the published tags (exactly what the README warns about, now with runner evidence). The job therefore builds the application images from the exact sources under test before running the documented commands, and the stale-image blocker is recorded: unlock = the owner's next authorized publish-images dispatch (whose verify job now runs the architecture inspector and a pull-and-execute check)."
   ],
   "remaining_scope": [
    "Implement and verify the approved scoped pairing/session credential lifecycle (UX-AUTH-007/008, API-002, DEP-004) after ADR 0005 receives qualified independent review: provision/pair, rotate, revoke, expire, and cross-Vault suites plus browser and desktop evidence.",
    "Pass E2E-008 against a real HTTPS hostname with a CA-issued certificate (the TLS topology, redirect, health/readiness, and PUT/DELETE round trip are locally rehearsed and verified; the remaining step needs an operator-owned public domain and certificate), plus the gateway/web log observability sign-off and operator game day.",
    "DEP-007 release-execution note: the inspection tool and pull-and-execute step are wired into the publish workflow's verify job; they execute on the owner's next authorized release dispatch (external gate).",
    "DATA-006 bounded listing and any remaining DB-* hardening follow the sequence-5 scoped schema after sequence 4 approval."
   ],
   "evidence": [
    "services/api/src/lib.rs",
    "deploy/containers/api/Dockerfile",
    "deploy/containers/worker/Dockerfile",
    "deploy/containers/migrator/Dockerfile",
    "deploy/containers/backup/Dockerfile",
    "deploy/containers/restore/Dockerfile",
    "deploy/containers/validator/Dockerfile",
    "deploy/compose/compose.yaml",
    "tests/smoke/api.sh",
    "Makefile",
    "docs/DEPLOYMENT.md",
    "docs/OPERATOR-GUIDE.md",
    "packages/storage/postgres/src/lib.rs",
    "docs/adr/0005-scoped-pairing-session-lifecycle.md",
    "contracts/protocol/pairing-session-v1.json",
    "contracts/protocol/pairing-session-v1.schema.json",
    "tools/lint/check-pairing-session.py",
    "tests/contracts/test_pairing_session.py",
    "docs/runbook.md",
    "docs/OPERATOR-GUIDE.md",
    "contracts/web/security-headers-v1.json",
    "contracts/web/security-headers-v1.schema.json",
    "tests/smoke/headers.sh",
    "deploy/web/nginx.conf",
    "tools/lint/check-foundation-pins.py",
    "tools/release/inspect-image.py",
    "tools/lint/check-compose-topology.py",
    "packages/storage/postgres/Cargo.toml",
    "deploy/gateway/envoy.yaml",
    "deploy/containers/postgres/init-roles.sh",
    "services/api/src/main.rs",
    "deploy/compose/.env.example",
    "services/api/src/lib.rs",
    "packages/storage/persistence/src/lib.rs",
    "tests/integration/backup-restore.sh",
    "services/restore/src/main.rs"
   ]
  },
  {
   "sequence": 9,
   "work_package": "Implement four-native packages, signing, updater, evidence",
   "primary_ids": [
    "REL-*",
    "INST-*",
    "UPD-*"
   ],
   "status": "Partial",
   "completed_scope": [
    "Local slice: the desktop app builds as a real native package for the current host — Veyora_1.0.0_aarch64.dmg (5.2 MiB, Mach-O thin arm64, ad hoc linker-signed) via `make desktop-build`, with a SHA-256-pinned evidence record at release/evidence/native-preview/macos-arm64-preview.md stating exactly what is and is not proven (no signing/notarization credentials locally, one of four targets, no updater corpus). INST-006's path conventions are unit-asserted in the desktop shell (suggested_vault_dir_from: Documents first, app-data fallback, bare-name last resort — never a bundle/download/checkout path; veyora-desktop 10/10)."
   ],
   "remaining_scope": [
    "Sign and notarize packages (requires Developer ID credentials), build all four native targets (macOS Intel, Windows AMD64/ARM64 need their hosts), and pass E2E-009 on real hardware with final candidate bytes (INST-001..007, UPD-001..004)."
   ],
   "evidence": [
    "release/evidence/native-preview/macos-arm64-preview.md"
   ]
  },
  {
   "sequence": 10,
   "work_package": "Select OSI license and complete public/community docs",
   "primary_ids": [
    "OSS-*",
    "DOC-*"
   ],
   "status": "Partial",
   "completed_scope": [
    "OSS-005 community surface (preview-coherent, intake-closed state): public governance.md (decision model by class, maintainer expectations, contribution state and the prerequisites for opening intake), roadmap.md (a view over the PRD gates — explicitly not a second source of truth — with honest near-term blockers and a not-on-the-roadmap section), support.md (routing table incl. security-first handling and 'what support will never ask for'), bug and documentation issue templates, and a PR template that requires PRD/registry IDs and executed-green evidence; SECURITY.md and CODE_OF_CONDUCT.md already existed. OSS-002 remains satisfied (every public surface says source-available, never open source). OSS-006's machine-readable inventory groundwork exists (docs/legal, dependency lockfiles) pending the formal SBOM scan.",
    "OSS-002 public-surface scan machine-enforced: tools/lint/check-source-available-wording.py (in `make check`) scans all 36 public markdown surfaces and fails on any 'open source' occurrence outside reviewed negation contexts (README license-section negation, the roadmap's never-claim-premise, OSI-approved license references, OSD citations), and on any file that mentions 'open source' without ever saying 'source-available'. PRD.md is excluded as the internal specification that defines these requirements. Negative-verified: a planted claim correctly fails the lint; the current tree passes."
   ],
   "remaining_scope": [
    "BLOCKER (owner/legal decision, cannot be made by an engineer): OSS-001/OSS-003 require the copyright owner to adopt an OSI-approved license and explicitly choose the project strategy (permissive e.g. Apache-2.0 vs network copyleft e.g. AGPL-3.0, with dependency and contribution implications). Until that decision lands, PolyForm Noncommercial stays and all surfaces must keep source-available wording (OSS-002 enforced). Unlock: recorded owner decision + license scan + manifest/badge/notice updates in one change.",
    "After the license decision: rerun the public-surface scan, refresh dependency/SBOM inventory (OSS-006), and dry-run the contributor workflow (OSS-004) before opening intake."
   ],
   "evidence": [
    "docs/governance.md",
    "docs/roadmap.md",
    "docs/support.md",
    ".github/ISSUE_TEMPLATE/bug-report.md",
    ".github/ISSUE_TEMPLATE/docs-issue.md",
    ".github/pull_request_template.md",
    "tools/lint/check-source-available-wording.py"
   ]
  },
  {
   "sequence": 11,
   "work_package": "External assessment, soak, Beta remediation, Stable decision",
   "primary_ids": [
    "SEC-ASSURE-*",
    "PERF-*",
    "G7"
   ],
   "status": "Partial",
   "completed_scope": [
    "SEC-ASSURE-004 complete: every remote `uses:` in .github/workflows is now a full 40-character commit SHA resolved from each action's published tag via the GitHub API (checkout, setup-node, setup-python, rust-toolchain@stable, rust-cache, upload-artifact, and the five docker/* actions), each annotated with its original mutable ref for readability, and machine-enforced by tools/lint/check-actions-pinned.py in `make check` (refuses mutable tags, bare defaults, and short SHAs; local composite actions exempt).",
    "SEC-ASSURE-005 local half: fuzz-boundary tests for the security kernel's parsing surfaces (kernel-core tests/fuzz_boundaries.rs) — a seeded 20k-input generator drives the recovery-kit decoder and canonical CBOR codec with the no-panic/no-silent-acceptance/canonical-round-trip contracts, structured near-miss classes (truncation, case corruption, hyphen normalization bait, checksum-breaking mutations) plus a retained regression corpus that found and pinned real behavior on its first run (the empty definite CBOR array is canonical and must round-trip — kept as the positive control; truncated/nested-overflow forms all fail closed). fmt/clippy -D warnings clean; the whole security-kernel workspace passes 15/15 test binaries. Scheduled long-run fuzzing in CI remains the runner-side half.",
    "PERF local-reference instrument and first measurements: tools/release/bench-client.mjs measures PERF-002 (kernel load + self-test per navigation), PERF-004 (the synchronous search pipeline over a 10,000-item fixture, plus the user path including debounce and a keystroke-backlog check), and PERF-005 (save click → acknowledged panel) against the live stack, writing a JSON artifact beside release/evidence/perf/README.md. First run on this host (local-reference-2026-09-06.json): PERF-002 p95 169 ms (target 1000), PERF-004 pipeline p95 25 ms with no backlog (target 100), PERF-005 user-path p95 2811 ms — over target, root-caused to the unwindowed full-table re-render after save with the large fixture, and filed as finding PERF-F-001 against PERF-005/006 in the evidence note (the harness is the reproducible instrument for the fix and for reference-hardware CI runs; reference-hardware acceptance remains external).",
    "PERF-F-001 resolved and PERF-005 now meets its target locally: the table renders in a 500-row window with a Show-more control (search/scope changes reset the window), and the save acknowledgment panel defers the table re-render on large vaults until the panel closes while small vaults keep the immediate refresh. The fix's own regression run exposed and fixed a window-shrink defect in the first cut (window state collapsed to a filtered list's length and capped later renders). Post-fix measurements (local-reference-2026-09-07-windowed.json): PERF-005 acknowledgment p95 32 ms (was 2811 ms; target 250), PERF-004 pipeline p95 8 ms with no backlog (user path incl. debounce 158 ms), PERF-002 p95 187 ms (target 1000). The harness was corrected in the same pass — the acknowledgment figure now dispatches the save click in-page because Playwright's click actionability added ~300 ms of driver overhead, with the driver-click compose path reported separately. Full browser regression green after the render change (comprehensive 70/70 twice consecutively; smoke, a11y 19/19, keyboard 20/20, journeys 6/6, fault 4/4; 108/108 web units). Reference-hardware acceptance remains CI-side.",
    "SEC-ASSURE-003 CI-side half COMPLETE on real runners: a new security-scan workflow (.github/workflows/security-scan.yml — push + weekly + nightly + manual, all actions SHA-pinned) runs the secret scan, cargo-audit (vulnerabilities deny; the 18 current warnings are all unmaintained notices for the GTK3 family Tauri's webkit2gtk bindings require plus other no-replacement transitives, each ignored with crate-mapped rationale in .cargo/audit.toml and revisit-on-upgrade notes — verified locally and on the runner), a machine-readable third-party license inventory (new tools/release/generate-license-inventory.py: 544 dependencies across cargo+npm from cargo metadata --locked plus the root lockfile, forbidden-license policy, zero NOASSERTION), the actions-pinning lint, and uploads the inventory as an artifact. SEC-ASSURE-005 CI-side half COMPLETE: the workflow's nightly fuzz job runs the kernel fuzz-boundary suite with a date-derived rotating seed plus the full kernel workspace as a regression guard. First runner run green end-to-end (Security scan run 34063175852 success, both jobs; CI run 34063175864 success alongside). The .cargo tooling-config directory is declared in the layout manifest so the closed-root gate keeps enforcing.",
    "Every browser suite the goal requires now executes on GitHub runners on every push: the E2E-002..007 acceptance-journey suite joined the Compose-integration job (own pristine stack reset after the keyboard audit), and a dedicated fault-injection job runs the 429/503/latency suite against its own rate-limited, delay-injected disposable stack with teardown. Runner evidence on the first green run (CI 34067233556, all seven jobs success): 'E2E journeys: 6 passed, 0 failed' and 'Fault suite: 4 passed, 0 failed' alongside the existing smoke/comprehensive/a11y/keyboard/token-gate/backup-drill steps — the full browser acceptance surface is now continuously verified, not just locally."
   ],
   "remaining_scope": [
    "SEC-ASSURE-001/002/006 and the external assessment: require the sequence-4 review outcome (reviewer appointment is the owner's step; package and record kit are ready) and audit-scope claims tied to it.",
    "PERF-001..006 reference-hardware acceptance runs (the local instrument and reference measurements exist; acceptance is judged on the specified hardware) and the soak/Beta/Stable promotion decision."
   ],
   "evidence": [
    "tools/lint/check-actions-pinned.py",
    "packages/security-kernel/crates/kernel-core/tests/fuzz_boundaries.rs",
    "tools/release/bench-client.mjs",
    "release/evidence/perf/README.md",
    "release/evidence/perf/local-reference-2026-09-06.json",
    "release/evidence/perf/local-reference-2026-09-07-windowed.json",
    ".github/workflows/security-scan.yml",
    "tools/release/generate-license-inventory.py",
    "release/license-inventory.json",
    ".cargo/audit.toml"
   ]
  }
 ]
}
